# 开发日志

本文件用于持续记录每次开发任务的目标、实现过程、故障、根因、解决方案、验证结果和后续注意事项。每完成一个独立任务，在文件顶部的“任务索引”增加一行，并在末尾追加一条完整记录。

## 记录规则

- 每项任务使用唯一编号：`TASK-YYYYMMDD-NNN`。
- 区分“离线测试通过”和“真实系统验证通过”，不得混写。
- 记录可观察现象、报告错误和根因，不只写“已修复”。
- 写明安全边界、尚未覆盖的分支和禁止假设的内容。
- 不粘贴 Cookie、Token、完整真实 Payload、真实客户资料或原始 HAR。
- 修复后必须记录回归测试和对应 Git 提交/标签。
- 新任务开始前先阅读最近一条日志的“下次注意事项”。

## 任务索引

| 任务编号 | 日期 | 任务 | 结果 | 版本/标签 |
|---|---|---|---|---|
| `TASK-20260814-001` | 2026-08-14 | Excel 导入并自动创建、填写多个运单页签 | 6/6 实站填表成功 | `v0.1.6` / `automation-fill-success-v0.1.6` |
| `TASK-20260814-002` | 2026-08-14 | 保存后保留成功页签并稳定创建下一单 | 离线与浏览器模拟通过 | `v1.0.4` |
| `TASK-20260814-003` | 2026-08-14 | 捕获网站缓存 fetch 的保存响应并导出诊断 | 离线与浏览器模拟通过 | `v1.0.5` |
| `TASK-20260814-004` | 2026-08-14 | 避开保存后刷新并安全重绑新订单表单 | 离线与浏览器模拟通过 | `v1.0.6` |
| `TASK-20260814-005` | 2026-08-14 | 兼容相同到站的跨订单候选DOM复用 | 离线与浏览器模拟通过 | `v1.0.7` |
| `TASK-20260814-006` | 2026-08-14 | 恢复联想控件状态并对未渲染候选做一次受控重试 | 离线与浏览器模拟通过 | `v1.0.8` |
| `TASK-20260814-007` | 2026-08-14 | 按真实 `.fn-dropdown__menu` 锁定焦点候选浮层 | 离线与浏览器模拟通过 | `v1.0.9` |
| `TASK-20260814-008` | 2026-08-14 | 相同到站逐字重触发并为两个枚举控件增加焦点重试 | 离线与浏览器模拟通过 | `v1.0.10` |
| `TASK-20260814-009` | 2026-08-14 | 按DataList/Select主动呼出菜单并验证连续四单 | 离线与浏览器模拟通过 | `v1.0.11` |
| `TASK-20260814-010` | 2026-08-14 | 保留新运单自动打开的到站菜单并分阶段打开其他菜单 | 离线与浏览器模拟通过 | `v1.0.12` |
| `TASK-20260814-011` | 2026-08-14 | 已活动的新运单页签零点击切换并记录焦点状态 | 离线与浏览器模拟通过 | `v1.0.13` |
| `TASK-20260814-012` | 2026-08-14 | 正式串行流程最终实站验收与故障链路归档 | 6/6 保存、72/72 字段通过 | `v1.0.13` |
| `TASK-20260814-013` | 2026-08-14 | 界面重做为应用式面板并迁移交付目录（引擎零改动） | 离线与浏览器模拟通过，实站已回归 | `v1.1.0` |
| `TASK-20260814-014` | 2026-08-14 | 鲁棒性加固：断点续跑、关闭确认、慢网络超时放宽 | 离线与浏览器模拟 23/23 通过，实站已回归 | `v1.2.0` |
| `TASK-20260814-015` | 2026-08-14 | 修正展开订单明细后窗口长出屏幕的定位问题 | 浏览器模拟 13/13 通过（含对照组），实站已回归 | `v1.2.1` |
| `TASK-20260814-016` | 2026-08-14 | v1.2.1 实站回归验收，解除三项待验证状态 | 用户确认实站测试通过 | `v1.2.1` |
| `TASK-20260815-001` | 2026-08-15 | 到站确认放宽：选中标志 + 非空回显即成功 | 离线测试通过；实站 4/4 验证通过 | `v1.4.1` |
| `TASK-20260815-002` | 2026-08-15 | 明细滚动修复 + 人工卡片显示字段期望值 | 离线测试通过，实站回归待做 | `v1.4.2` |
| `TASK-20260815-003` | 2026-08-15 | 订单明细表格内部滚动（限高 320px） | 离线测试通过，实站回归待做 | `v1.4.3` |

---

## TASK-20260814-001：多运单自动创建与填表初测

### 1. 任务目标

创建一份包含 6 条虚构订单的 Excel，并开发油猴脚本，在车满满系统中每批创建 2 个内部开单页签并自动填写。第一阶段只验证导入、映射、页签创建和填值，不允许保存运单。

### 2. 范围与安全边界

本次包含：

- 一份 Excel、6 条虚构数据、3 个工作表。
- 送货/自提各 3 条，付款方式统一为现付。
- 完整数据以及空手机号、空包装、空重量、空体积等情况。
- 每批 2 条，共 3 批。
- 字段级定位、写入和回读报告。

本次不包含：

- 自动保存或并发保存。
- 到付、欠付、回付和返款组合。
- 重名地点的自动消歧。
- 真实客户资料或生产订单。

脚本从第一版开始拦截保存按钮和 `/coHandle`；后续补充了 `保存(F9)`、F9 键盘、Fetch 和 XHR 四类保护。

### 3. 交付内容

- `油猴多运单填表初测.user.js`
- `油猴多运单填表测试数据.xlsx`
- `使用说明.md`
- 字段级 CSV 核对报告导出功能

### 4. 开发与迭代记录

#### v0.1.0：离线原型

- 实现本地 XLSX ZIP/XML 解析，不依赖远程 CDN。
- 导入 6 条订单并做缺列、重复 ID、非法件数、未知枚举和公式单元格预检。
- 根据中文标签查找页面控件。
- 每批点击两次“创建运单”并填写两个内部页签。
- 在本地模拟页面完成 6 条、每条 12 个字段的回读测试。

离线测试成功，但真实网站第一次运行全部停在到站字段。

#### v0.1.1：使用 `data-path` 精确定位

真实页面并不适合依赖中文标签层级。通过用户提供的空白输入框 HTML，确认了稳定字段标识：

| 领域字段 | 网站 `data-path` |
|---|---|
| 到站 | `arr` |
| 送货方式 | `delivery_mode` |
| 发货人 | `cor_name` |
| 收货人 | `cee_name` |
| 收货人手机号 | `cee_mobile` |
| 货物名称 | `name_1` |
| 包装 | `pkg_1` |
| 件数 | `num_1` |
| 重量 | `weight_1` |
| 体积 | `volume_1` |
| 运费 | `co_freight_f` |
| 付款方式 | `pay_mode` |

修复后，脚本优先按 `data-path` 定位，中文标签只作为兼容回退。

#### v0.1.2：增加真实选择状态验证

用户提供了手工选中后的控件状态：

- `data-is-edited="1"`
- `data-is-select="1"`
- `value` 和 `title` 为选中后的展示值

据此确认：输入框中出现文字不代表网站已经接受该业务对象。脚本改为只有看到 `data-is-select="1"` 才判定到站、送货方式或付款方式成功。

#### v0.1.3：动态查找精确候选

最初候选选择器依赖常见的 `li`、`role="option"` 等结构，真实网站不匹配。脚本改为：

1. 读取当前 Excel 行的到站文本。
2. 记录输入前可见的 DOM。
3. 输入后寻找新出现且文本完全一致的最深层节点。
4. 只在唯一匹配时点击；多个匹配则停止。

该逻辑不硬编码“通海县”，可以处理 Excel 中的其他地点；但当时点击后仍未得到网站确认。

#### v0.1.4：保持焦点并自动采集诊断

发现普通文本填写流程会立即触发 `blur`。车满满 DataList 在失焦后关闭，导致候选框消失。修复为：

- 到站填写期间保持焦点。
- 不在候选选择前触发 `blur`。
- 仍失败时自动收集候选 DOM 摘要，不再要求人工捕捉瞬间消失的列表。

#### v0.1.5：模拟完整用户点击事件

报告显示脚本已找到并点击候选，但网站仍未设置 `data-is-select="1"`。根因是 `.click()` 只产生 click 事件，而网站控件可能在 `pointerdown` 或 `mousedown` 阶段完成选择。

修复为模拟完整事件序列：

```text
pointerover → pointerdown → mouseover → mousedown
→ mouseup → pointerup → click
```

同时自动记录点击目标及四层父节点 HTML，以便后续诊断。

该版本使真实系统中的到站选择成功。

#### v0.1.6：复用枚举选择并修正报告

到站成功后，下一失败点移动到 `delivery_type`：找不到“送货/自提”枚举。送货方式和付款方式都是网站自定义 Select，因此复用动态候选精确选择机制：

- `delivery → 送货`
- `pickup → 自提`
- `pay_billing → 现付`

同时修复报告问题：旧版会把整条记录的最终错误重复写到此前已经成功的字段上。新版只在失败字段自己的行中记录错误。

### 5. 主要问题、根因和解决方法

| 问题 | 可观察现象 | 根因 | 解决方法 |
|---|---|---|---|
| 到站控件无法定位 | 6 条均报“未找到唯一控件” | 真实 DOM 使用 `data-path`，中文标签不在预期层级 | 使用 `data-path="arr"` 等稳定属性定位 |
| 到站文字出现但仍失败 | 输入框显示通海县，报告仍失败 | 只写入了显示值，没有选择地点对象 | 点击真实候选，并要求 `data-is-select="1"` |
| 候选列表无法找到 | 报“未找到唯一的精确候选” | 候选 DOM 不是常见 `li/option` 结构 | 监听输入后新出现的 DOM，按唯一精确文本选择 |
| 候选框瞬间消失 | 无法复制候选 outerHTML | 提前触发 `blur` 关闭 DataList | 选择完成前保持输入框焦点 |
| 找到候选但网站不确认 | 报“候选已点击，但网站未确认” | `.click()` 没有触发控件依赖的完整指针事件 | 模拟 pointer/mouse 完整事件序列 |
| 送货方式继续失败 | 到站成功后报找不到送货/自提 | 自定义 Select 与 DataList 有相似动态候选行为 | 复用动态候选机制处理 `delivery_mode/pay_mode` |
| 报告显示成功字段也有错误 | `destination_text` 行显示 delivery 错误 | 导出时将记录级错误填入每个字段 | 错误只写入对应失败字段 |
| 保存保护未覆盖真实按钮 | 网站按钮为“保存(F9)” | 初始规则只匹配“保存” | 支持“保存(F9)”并拦截 F9、Fetch、XHR |

### 6. 验证结果

离线验证：

- 6 条订单、每条 12 个字段全部通过回读。
- 重复 ID、非法件数、未知枚举和公式单元格均在发起页面操作前被阻断。
- 空值未写成字符串 `null`、`undefined` 或数值 `0`。
- 保存按钮、F9、Fetch 和 XHR 保存路径均被拦截。

真实系统验证：

- 用户确认 6/6 条虚构订单全部成功创建内部页签并完整填入。
- 到站、送货/自提和现付均由网站设置 `data-is-select="1"`。
- 本次没有点击保存，没有验证服务端订单创建。

### 7. 关键经验

1. 自定义业务控件的显示值不等于业务状态。必须验证网站自己的选中标记或底层对象。
2. 优先使用稳定语义属性（如 `data-path`），不要把中文标签层级或 DOM 序号当成主定位方式。
3. 自动化下拉框不能默认选第一项；必须唯一、精确匹配，歧义时停止。
4. 合成 `.click()` 不一定等价于用户操作，需要了解控件真正监听的事件阶段。
5. 自动化脚本应自己采集诊断证据，避免要求操作者手工捕获短暂出现的 DOM。
6. 报告必须区分字段级错误和记录级错误，否则会误导排查方向。
7. 离线模拟只能验证脚本结构；私有业务系统必须经过真实页面的小批量验证。
8. 每修复一个字段都要让队列继续推进，以暴露下一个真实失败点，而不是假设其他字段相同。

### 8. 下次任务开始前注意事项

- 先检查网站脚本版本与字段 `data-path` 是否变化。
- 首批只运行 1～2 条，确认报告后再扩大到 6 条。
- 保留 `data-is-select="1"` 验证，不得为了“通过测试”放宽为只检查 `value`。
- 新地点若出现多个同名精确候选，必须增加行政区消歧，不可点击第一项。
- 新增付款方式前，先取得对应成功保存样本和金额字段规则；不要只扩展 Excel 枚举。
- 开始保存测试前，先验证查询/去重、订单号生命周期、`errno=320` 和测试单撤销流程。
- 未完成保存安全门禁前，不移除保存按钮、F9 或 `/coHandle` 拦截。
- 每次修改核对报告格式后，检查成功字段、失败字段和记录级状态是否一致。
- 继续使用虚构姓名、测试手机号和无真实业务关系的金额。

### 9. Git 记录

- 初始成功提交：`088fd2c 自动创建并填写表格—成功`
- 成功标签：`automation-fill-success-v0.1.6`
- 状态记录：`TEST_STATUS.md`

## 2026-08-14 — v0.1.7 单运单自动保存测试版

根据重新确认的最终目标，浏览器脚本后续需要完成批量创建、填写和保存；手机工具负责拍照、AI/OCR 提取、人工核对并导出 Excel。

本次在不修改 v0.1.6 稳定脚本的前提下新增单条保存测试版：

- 强制 Excel 中只能有一条订单；
- 必须勾选“获准的虚构测试订单”确认框；
- 所有字段回读通过后才查找并点击保存按钮；
- 每个页面生命周期只允许一次脚本保存尝试；
- 只放行第一次由脚本触发的 `/coHandle` 请求，额外保存仍被拦截；
- 根据响应分类为保存成功、保存失败、需要人工决定或结果不确定；
- `errno=320` 不自动继续；
- 超时、断网或响应不可解析时禁止自动重试；
- 检测到 v0.1.6 同时运行时阻断测试并提示停用旧脚本；
- 报告增加保存通道、HTTP 状态、`errno` 和脱敏消息字段。

离线已完成 JavaScript 语法检查、响应分类测试、单条/多条导入门禁测试以及单行 XLSX 结构与视觉检查。

真实网站验证结果：用户确认 v0.1.7 成功创建订单页签、填写字段并保存订单。阶段 B“单条自动保存”完成。该结果不外推为串行批量保存或并行保存已通过，下一步进入串行批量保存开发。

## 2026-08-14 — v0.1.8 串行批量自动保存测试版

按用户要求直接在 v0.1.7 上升级，保留原油猴命名空间，使安装新版时能够覆盖单条版本。

主要变化：

- Excel 从恰好一条改为初测最多两条；
- 整批只能启动一次；
- 每条依次执行创建、填写、回读、保存和结果确认；
- 上一条保存结束后增加短暂等待，再开始下一条；
- 填写失败或网站明确拒绝时记录该条结果，并继续下一条；
- `errno=320`、超时、断网、HTTP 异常或响应不可解析时停止剩余队列；
- 每条只放行一次由脚本触发的 `/coHandle` 请求；
- 面板显示整批进度、成功数、异常数和未处理数量；
- 页面生命周期内不允许第二次启动批次或更换导入文件；
- 新增两条虚构订单的专用测试 Excel，分别覆盖送货和自提。

离线已完成语法检查、响应分类、两条导入、三条阻断和工作簿结构检查。真实网站串行保存仍待验证。

## 2026-08-14 — v0.2.0 单页面内部页签并行可行性探针

用户明确不接受通过新建浏览器标签页或窗口实现并行，因为这会扩大共享会话、运单号和操作管理风险。并行方向限定为同一车满满页面内的多个内部订单页签。

新增独立探针脚本，功能包括：

- 自动创建两个网站内部订单页签；
- 为两个表单写入不同的虚构发货人标记；
- 采集 12 个关键 `data-path` 的全局、连接和可见数量；
- 推断并追踪两个表单根容器；
- 在第二个页签活动时直接写入第一个非活动表单；
- 自动切回两个内部页签验证标记保持和相互隔离；
- 根据结构保留、隔离写入和切换持久性输出 `possible/not_possible_via_dom/inconclusive`；
- 完成后自动下载脱敏 JSON 报告；
- 全程拦截保存按钮、F9、Fetch 和 XHR 保存请求。

离线已完成 JavaScript 语法检查以及可行/不可行判定分支测试。真实页面 DOM 结论等待用户运行探针后的自动报告。

### v0.2.0 实站结果

用户提供的自动报告结论为 `possible`：

- 运行前已有一套开单表单；探针新建 A、B 后，全局每个关键字段为 3 份，符合“基线 1 + 探针 2”；
- A、B 使用不同的 `.order-content.skin-border` 根容器；
- 两套表单的 12 个关键字段均保持连接；
- 非活动根容器隐藏但未销毁；
- 直接写入非活动 A 表单成功，B 表单保持不变；
- 自动切回 A、B 后两边标记均保持。

该结果证明单页面双表单结构和普通文本隔离写入可行，但未验证隐藏自定义控件和保存请求。

## 2026-08-14 — v0.3.0 单页面内部页签并行最终探针

按用户要求将剩余验证合并为一次运行：

- 自动识别并记录运行前已有表单作为基线；
- 创建并绑定新增 A、B 两个内部页签；
- 同时填写两套表单的文本和数值字段；
- 分别验证隐藏表单的到站、送货方式和付款方式；
- 隐藏自定义控件失败时自动切换活动页签兜底，并保留失败证据；
- 保存前分别回读全部 12 个字段；
- 只有两套表单全部通过才放行保存；
- A 请求发出后立即切换 B 并触发第二次保存；
- 记录请求是否重叠、响应分类以及脱敏订单身份指纹是否不同；
- 最终报告不保存原始订单号、Cookie、Token 或请求体。

最终探针包含真实保存两条虚构订单的强制确认门禁。离线语法、结构判定、响应分类、身份哈希和最终结论测试已通过，实站结果待验证。

## 2026-08-14 — v1.0.0 正式批量并行自动填写保存

依据 v0.3.0 最终探针报告实施正式版，不再增加探索性探针。

正式调度结构：

- 每份 Excel 允许 1–100 条，固定每组最多两条；
- 顺序创建并绑定两个脚本自建内部页签；
- 两套独立表单并行填写9个普通文本/数值字段；
- 到站、交货方式和付款方式通过全局活动页签互斥锁依次操作；
- 每单保存前重新回读全部12字段；
- A 保存请求被观察到后，以40ms页签稳定等待切换到 B 并触发第二次保存；
- 两次响应与各自保存许可绑定，即使响应乱序也不会串单。

正式安全和恢复能力：

- 未授权保存按钮、F9和额外 `/coHandle` 请求继续拦截；
- `errno=320` 进入人工继续/跳过/放弃流程；
- 填写失败可人工修正后重新验证；
- 保存结果不确定停止新调度且禁止盲目重试；
- 未完成批次保存完整本地检查点，刷新后不自动执行；
- 明确保存记录写入 `batch_id + source_record_id` 防重复台账；
- 批次完成自动删除包含完整订单字段的检查点；
- 只关闭本批脚本创建并明确保存成功的页签。

验证结果：

- JavaScript 语法检查通过；
- 离线逻辑测试覆盖1/100/101条、Schema、公式、枚举、响应分类、重复保护、双任务选择和报告脱敏；
- 浏览器模拟中两单均完成12/12字段回读；
- 初次模拟发现第二条保存前350ms等待会使 A 先结束，已按实站探针改为40ms并确认请求最大同时在途数为2；
- 模拟第二条返回 `errno=320`，面板“继续保存”发出第三次受控请求并最终成功；
- 成功后仅脚本创建页签被关闭，原有页签保留；
- 完成后检查点删除、两条防重复记录写入；
- 刷新恢复把保存中任务转换为“结果不确定”，且不会自动发出请求。

## 2026-08-14 — v1.0.1 正式串行重构

发现决定性网站约束：如果当前新建运单尚未保存，再次点击创建运单会得到相同运单号；只有当前单保存成功后再创建，号码才会从例如 039 递增为 040。双内部页签虽然能隔离 DOM 和重叠请求，但不能保证订单身份唯一，因此不再适合作为正式调度架构。

本次重构：

- 将正式文件更名为 `油猴批量串行自动填写保存.user.js`，版本升至 v1.0.1，并移除易误装的并行正式文件名；
- 删除双任务波次、跨订单普通字段并行、双保存许可、40ms切换和重叠保存；
- 每次只执行一个订单，保存成功且自建页签确认移除后才取下一条；
- 在填写前从当前表单严格读取运单号，并与上一已保存订单比较；
- `errno=320` 删除“暂时跳过”，填写/保存异常均暂停当前批次；
- 保存成功改为“待关闭”中间态，关闭失败第一次即暂停；
- 检查点升至 v2，拒绝自动恢复 v1.0.0 并行检查点，同时继承旧防重复台账；
- 脱敏报告只记录运单号末三位和哈希。

验证包括语法检查、Schema 及100条门禁、单任务选择、单号规范化/递增、响应分类、重复保护、报告脱敏和浏览器串行模拟。

补充修正：正式串行版兼容读取 v1.0.0 防重复台账时，“解除所列重复保护”现在会同时删除新旧台账中的指定键，避免旧记录在重新预检时被再次合并、导致测试文件始终无法启动。

## 2026-08-14 — v1.0.2 空白开单页签接管

真实页面进入开单功能后本身可能已经存在一个空白“创建运单”页签。旧逻辑把运行前所有页签视为不可触碰基线，导致第一条任务再次点击创建，并可能产生多个共享同一运单号的空草稿。

修正后，脚本启动时逐个激活并检查“创建运单”页签：唯一空白页签直接接管；多个页签只有在号码完全相同且再次确认表单为空时才关闭重复项，保留一个用于第一条任务。非空、异号或无法确认的页签一律停止并交给人工处理。真实站点的 `.fn-icon-close` 已加入关闭控件定位。

## 2026-08-14 — v1.0.3 保存观察误判修正

实站第一条订单保存成功，但脚本未捕获保存请求，并在5秒后错误标记为明确保存失败。这说明网站运行时可能替换了脚本在 `document-start` 阶段包装的 Fetch/XHR，或使用了已缓存的网络函数。

修正后，每次保存前重新检查并包装当前 Fetch/XHR，同时用 Performance Resource 观察保存接口作为兜底。任何“点击过保存但没有得到可解析响应”的情况都归类为“结果不确定”，要求查询订单列表后确认，禁止按明确失败直接重试。

## TASK-20260814-002：v1.0.4 成功页签保留与下一单创建门禁

用户确认保存成功后的内部页签必须保留，并提供 `Har/完整流程.har` 及网站真实创建入口结构。HAR显示保存接口明确成功后，网站仍需约2秒才进入下一次开单流程；旧版在保存结束后立即做一次性文字查找，容易在页面过渡期报告“未找到创建运单按钮”。

本次实现：

- `errno=0` 后直接将订单标记为成功，并将对应页签/表单标为本批次已完成；不再进入成功待关闭状态，也不再自动关闭成功页签。
- 成功页签从后续空白草稿检查中排除；启动时既有同号空白草稿的安全清理逻辑继续保留。
- 按真实 DOM 优先定位 `i.fn-icon.fn-icon-file-add` 最近的链接，以精确“创建运单”文字作为回退。
- 保存响应完全结束后，最长等待15秒，且要求唯一入口连续稳定500ms；发现多个入口立即停止。
- 创建入口仅发送一次完整用户点击事件，并等待新增内部页签/表单。无法确认创建结果时进入人工处理，保留“已经尝试创建”检查点，防止盲目重复点击。
- 检查点升级至v3；v1.0.3 的 `saved_pending_close` 迁移成 `saved + retained`，迁移后立即持久化新检查点。

验证结果：

- JavaScript语法和正式版离线逻辑测试通过。
- 浏览器模拟将创建入口在保存成功后隐藏1.2秒，脚本确实等到入口恢复并稳定500ms后才创建下一单。
- 两条成功页签均保留，原有基线页签不受影响；任意时刻仍最多一个保存请求。
- 同号运单、`errno=320`、重复创建入口、现有空白页签复用、同号空白草稿清理、旧检查点迁移和未捕获响应分支均通过回归。

实站状态：尚待用户安装v1.0.4后用虚构订单验证。测试前只启用当前正式脚本；如果创建入口门禁暂停，应先检查页面是否已经出现新空白页签，再使用面板重查，避免人工重复创建。

## TASK-20260814-003：v1.0.5 缓存 fetch 响应捕获与诊断

实站现象：第一条订单实际保存成功，但脚本只从 Performance Resource 看到请求完成，没有从当前 Fetch/XHR 包装器取得响应正文，因此按安全规则标记“结果不确定”。

用户提供的 `Har/自动流程.har` 证明：

- 保存接口是 `/api/Order/Order/coHandle`，资源类型为 fetch；
- HTTP状态为200，响应类型为JSON，`errno=0`、`errmsg=保存成功`，总耗时约117ms；
- 调用栈由网站 `common...js` 和 `orderEditor...js` 的模块内部请求函数发起，脚本的 `triggerObservedSave` 只负责触发按钮点击；
- 因网站模块已保存其请求函数引用，保存前重新包装当前 `window.fetch` 不能覆盖该引用。

修正：

- 在页面真实执行环境中安装只针对 `/coHandle` 的观察器；
- 同时观察 Fetch、XHR、`Response.json()` 和 `Response.text()`，不修改响应值、不消费网站原始响应；
- Performance发现请求完成时增加1.5秒宽限，优先等待网站自己的响应解析动作，仍未取得正文才判为不确定；
- 新增脱敏诊断JSON，按时间记录许可建立、保存点击、捕获机制、HTTP/JSON结构元数据、errno和最终状态；不包含请求体、原始响应或个人信息。

验证：

- 语法和离线正式版测试通过；
- 浏览器模拟中，网站在脚本安装前缓存 fetch 的场景已由 `Response.json()` 旁路成功捕获；
- 第一条 `errno=0` 判定保存成功，第二条 `errno=320` 进入人工确认；
- 诊断报告中未出现测试姓名、手机号或到站文本；原有串行、运单号、页签保留和创建入口回归全部通过。

实站状态：等待用户安装v1.0.5后再次用虚构订单验证。如果仍出现结果不确定，应在刷新页面前点击“导出诊断JSON”并提供文件。

## TASK-20260814-004：v1.0.6 保存后刷新冷却与DOM重绑

v1.0.5实站结果证明保存响应旁路捕获已经生效：TEST-0001通过 `response.text` 得到HTTP 200、`errno=0`。但TEST-0002报告“订单页签或表单已从页面移除”。

诊断JSON与 `自动流程1.0.5.har` 对齐后确认：

- 第一单保存运单尾号065；
- 保存响应结束约0.53秒后，脚本触发 `/api/Order/Order/co`，网站成功创建尾号066；
- 约2秒后网站继续调用 `/oinfo`、`orderList` 等保存后刷新接口；
- 页面刷新替换了内部页签/表单DOM，任务仍持有旧引用，因而在第二单已经创建后误报填写失败。

修正：

- 在每次明确保存成功后增加至少3秒冷却，再开始唯一创建入口稳定检测；
- 记录创建阶段完整脱敏时间线，包括冷却、按钮稳定、单次点击、新表单出现、绑定和运单号捕获；
- 当任务页签或表单引用断开时，从当前活动表单重新读取完整运单号；只有与任务原号码完全一致、候选稳定500ms且不存在未完成任务冲突时才重绑；
- 重绑后逐项核对9个普通字段，仅补写不一致字段，然后继续3个自定义字段；无法确认完整号码时停止，不按尾号猜测。

验证：

- JavaScript语法与正式离线测试通过；
- 浏览器模拟中，保存后1.8秒发生网站刷新，脚本等待满3秒后才创建下一单；
- 人工替换当前订单整个表单根节点后，脚本按同一完整号码重绑成功，并生成 `task_context_rebound` 诊断事件；
- 原有缓存fetch响应捕获、`errno=320`、运单号递增、成功页签保留、重复草稿和检查点回归全部通过。

当前失败批次的处理：尾号066只是已创建但未确认保存的草稿。必须先人工关闭该草稿并在订单列表确认未生成，再选择“确认未生成并重排”；不得直接再次保存或创建。

## TASK-20260814-005：v1.0.7 复用候选DOM安全回退

v1.0.6实站中，保存冷却和DOM重绑问题已经解决：第二单成功创建并读取尾号067，但到站选择报告“未找到唯一候选”。

HAR证据：

- 第二单到站请求 `/api/Basic/Nsug/sug/` 返回HTTP 200、`errno=0`，包含10条POI；
- 搜索文本与响应候选内容能够精确匹配，后端没有失败；
- 两单的搜索文本哈希相同；网站跨订单复用了已有候选DOM节点；
- 旧算法只扫描“输入后新出现且此前不可见”的元素，因此复用节点即使内容正确、可点击，也被排除。

修正：

- 继续以新出现的唯一候选作为首选路径；
- 新候选为0时，检查所有精确文本候选，但只保留靠近当前控件、具有可见尺寸且通过 `elementFromPoint` 确认处于页面顶层的节点；
- 复用候选只有一个时允许点击；两个及以上立即停止，避免重名或陈旧面板造成误选；
- 诊断时间线记录选择模式 `new_dom/reused_dom`、各阶段候选数量、字段名和搜索值哈希，不记录实际到站。

验证：

- 语法和正式离线逻辑测试通过；
- 浏览器模拟让同一候选节点在第一单选择后保持可见，并在第二单改变/复用；第二单能够通过 `reused_dom` 回退选择；
- 缓存fetch、保存成功、`errno=320`、3秒冷却、运单号递增、表单根重绑、页签保留和诊断脱敏全部回归通过。

## TASK-20260814-006：v1.0.8 联想控件状态重置与受控重试

v1.0.7实站仍在第二单到站字段停止。新诊断明确报告4.5秒内没有任何可见精确候选；最新HAR则证明同一时段的联想接口已经成功返回精确候选。

证据与结论：

- TEST-0001保存成功，TEST-0002创建、绑定和运单号递增均成功，尾号由067变为068；
- 第二单目标查询于约82ms完成，HTTP 200、`errno=0`、10条POI、一个精确候选；
- 响应完成后页面仍没有精确文本节点，因此v1.0.7的新节点和安全复用节点两条路径均为0；
- 失败点是网站联想组件的前端状态/渲染，不是服务端查询，也不是运单创建或单号门禁。

修正：

- 为到站字段增加专用输入流程，不再沿用普通控件的连续 `input/change`；
- 每轮先聚焦并点击控件，清除 `data-is-select/title`，以原生value setter写空并发送输入事件，短暂等待后再写目标值；
- 首轮2.4秒未看到唯一安全候选时记录 `candidate_retry`，完整重置后再等待4.5秒；第二轮仍失败才转人工处理，禁止无限重试；
- 用Performance Resource Timing为联想阶段补充脱敏网络证据：请求数、完成数、最大耗时和可用状态码，不采集URL参数、请求体、响应正文或搜索文本。

验证：

- JavaScript语法检查与正式离线逻辑测试通过；
- 完整浏览器模拟耗时约74秒并通过；
- 新场景故意让第二单首轮到站查询不产生候选DOM，脚本记录一次 `candidate_retry`，第二轮复用同一候选节点并继续；
- 缓存fetch响应捕获、`errno=320`、保存后3秒冷却、运单号递增、DOM重绑、页签保留、候选唯一性及诊断脱敏全部回归通过。

实站状态：等待用户安装v1.0.8后继续使用当前测试数据验证。尾号068属于已创建但未确认保存的草稿，重测前应人工关闭并在订单列表确认未生成，再在面板选择“确认未生成并重排”。

## TASK-20260814-007：v1.0.9 焦点型候选浮层定位

用户通过页面检查确认到站下拉菜单的根节点类名为 `.fn-dropdown__menu`。菜单依赖输入控件焦点，切换到开发者工具后会立即消失，这解释了为何静态复制DOM困难，也说明自动化过程中不得在候选点击前触发失焦。

修正：

- 保持v1.0.8的“聚焦、点击、清空、输入”流程以及不发送提前 `change/blur` 的约束；
- 扫描候选时先收集所有可见 `.fn-dropdown__menu`；浮层内存在精确文本时，只在这些精确节点中执行新节点/复用节点唯一性判断；
- 浮层中不存在精确文本时才保留全页面安全回退，以兼容枚举控件或网站后续结构变化；
- `candidate_selected/candidate_retry/candidate_timeout` 诊断记录浮层总数、可见数和精确候选数，不保存DOM、完整文本或个人数据。

验证：

- JavaScript语法与离线正式测试通过；
- 浏览器模拟中的联想和枚举菜单均改为真实 `.fn-dropdown__menu` 类；
- 首次响应不渲染、二次重试、候选DOM复用、唯一性检查、缓存fetch、保存响应捕获和严格串行回归全部通过。

实站状态：等待用户安装v1.0.9验证真实焦点菜单选择。

## TASK-20260814-008：v1.0.10 三种下拉交互恢复

v1.0.9实站的第一单三种下拉均成功，第二单尾号069在到站步骤停止。HAR证明到站两轮查询均成功返回精确候选，但诊断显示两轮都没有 `.fn-dropdown__menu`；送货方式和付款方式尚未进入执行阶段。

修正：

- 到站首轮仍使用完整值，失败后的唯一重试改为从空值开始逐字输入，每个字符分别发送键盘和输入事件；
- 允许网站在没有菜单DOM时自行确认选择，但必须同时满足 `data-is-select=1` 和回读值精确一致，并记录 `candidate_auto_confirmed`；
- 送货方式与付款方式首次点击1.8秒无唯一菜单时，记录 `choice_retry`，失焦180ms重置单例浮层状态，再聚焦点击一次；
- 所有路径仍只允许一次重试，保持唯一候选和网站确认值门禁。

验证：

- JavaScript语法和离线正式测试通过；
- 完整浏览器模拟约79秒并通过；
- 模拟故意让第二单到站首轮完整查询、送货方式首次点击、付款方式首次点击都不产生菜单；逐字输入和两次焦点重试均成功恢复；
- 保存响应捕获、严格串行、运单号递增、3秒冷却、DOM重绑、候选复用和诊断脱敏回归通过。

实站状态：等待用户安装v1.0.10验证尾号070之后的真实批次。

## TASK-20260814-009：v1.0.11 主动呼出三类菜单与四单回归

用户补充真实控件结构：`arr` 是 `data-field-type="DataList"`，`delivery_mode` 是 `data-field-type="Select"`。这表明菜单可以通过控件交互主动呼出；先前诊断中的菜单总数为0，只能证明脚本当时没有成功打开，不能证明网站没有菜单能力。

修正：

- 新增统一的主动打开函数，针对控件发送focus、pointerover/down/up、mouseover/down/up、click完整序列；
- 鼠标序列后仍没有可见 `.fn-dropdown__menu` 时，仅补发一次 `ArrowDown` 的keydown/keyup，不发送Enter，不接受高亮项；
- DataList在清空前和目标查询响应后均执行打开检查；Select在首次及唯一重试前执行；
- 诊断新增 `dropdown_open_attempt`，记录控件字段类型、打开阶段、采用路径及各阶段可见菜单数量。

验证：

- JavaScript语法和离线正式测试通过；
- 完整浏览器模拟约140秒并通过；
- 新增连续四单场景，模拟控件只有收到mousedown才允许打开，并故意丢弃第2、3、4单三类菜单的第一次渲染；
- 四单12字段全部通过，保存请求为4且最大并发为1，运单号按039、040、041、042递增；
- 原有保存响应捕获、3秒冷却、单号门禁、DOM重绑、重复草稿、人工分支和诊断脱敏回归通过。

实站状态：等待用户安装v1.0.11连续测试至少4条。

## TASK-20260814-010：v1.0.12 避免重复点击关闭自动菜单

用户观察到第二单创建后到站菜单已经自动出现，但v1.0.11执行主动呼出时菜单又关闭，随后进入逐字输入。真实业务补充确认：第一单及所有后续新运单都会默认聚焦到站并自动显示菜单。

根因修正：

- 旧版把完整pointer/mouse/click事件同步发完，只在全部结束后检查；如果网站在mousedown打开、在后续click再次切换，就只能看到最终菜单为0；
- 新版在活动元素就是当前到站控件且菜单可见时直接复用，完全不发送点击；
- 需要主动打开时把事件拆成阶段并在各阶段等待检查；mousedown已打开且释放后仍可见时不再发送click；
- Select字段不再以“存在任意可见菜单”为成功，而是要求菜单中已经出现当前目标候选，排除到站选择后遗留的空菜单壳。

验证：

- JavaScript语法与离线正式测试通过；
- 浏览器模拟新增“每次创建后到站自动聚焦且菜单已显示，额外click会将其关闭”的模型；
- 连续四单039、040、041、042全部保存，12字段全部通过，最大保存并发为1；
- 四个到站控件收到的脚本额外click总数严格为0；完整回归约142秒通过。

实站状态：等待用户安装v1.0.12验证真实自动菜单保留路径。

## TASK-20260814-011：v1.0.13 新运单活动状态确认

用户指出新建运单可能只有切换到新页签后才会默认选中到站并显示菜单。代码核对发现：创建流程通过活动页签/可见表单绑定新订单，但后续 `switchToTask()` 总是再次点击页签；若页签本来已经活动，这次多余点击可能造成到站失焦。

修正：

- 创建绑定完成后、读取单号和填写字段前，先执行一次订单上下文确认；
- `switchToTask()`同时检查目标页签是活动页签且目标表单是唯一可见表单；两者满足时不发送任何页签事件；
- 只有目标订单确实未活动时才点击页签并等待活动状态；
- `new_form_bound`记录绑定时的页签/表单活动状态、焦点字段和菜单数；`task_switch`记录是否点击及切换时状态。

验证：

- JavaScript语法和离线正式测试通过；
- 连续四单模拟中每次创建后自动激活页签、聚焦到站并显示菜单；
- 所有 `task_switch` 均未点击，页面记录的活动页签重复点击和到站额外点击均为0；
- 039、040、041、042全部完成12字段和保存，最大保存并发为1；完整回归约146秒通过。

实站状态：等待用户安装v1.0.13验证创建后切换、焦点和菜单时间线。

## TASK-20260814-012：v1.0.13 正式流程最终验收与故障链路归档

### 1. 最终结果

用户使用正式 v1.0.13 连续执行 TEST-0001～TEST-0006，导出的脱敏 CSV 显示：

- 6/6 条订单全部保存成功，无失败、结果不确定或人工待处理；
- 每条 12/12 字段均通过回读，合计 72/72；
- 6 次保存均为 HTTP 200、`errno=0`，捕获通道为 `response.text`；
- 运单号后缀从 071 到 076 连续递增；
- 身份指纹互不重复，报告未泄露姓名、手机号或完整运单号。

因此，当前 Excel Schema v1.0、1～100 条门禁下的正式批量串行主流程全部功能测试成功。

### 2. 本轮主要卡点与根因

1. **并行创建会复用运单号。** 网站只有在上一单保存后才推进号码；未保存时继续创建得到相同号码，因此内部页签虽然能够并行填写，也不能用于正式订单创建。
2. **启动页面可能已有空白草稿。** 直接点击创建会多生成一个同号草稿；多个同号空白页签还会让任务绑定失去唯一性。
3. **保存实际成功但脚本看不到响应。** 网站模块提前缓存了 `fetch` 引用，运行时仅替换 `window.fetch` 无法截获该请求，早期版本把成功订单误判为保存失败或结果不确定。
4. **保存后的异步刷新替换表单 DOM。** 第一单响应结束后，网站仍会刷新订单列表和 `/oinfo`；脚本过早创建下一单时，持有的表单引用会被替换并误报页面移除。
5. **第二单下拉菜单反复失败。** 到站是焦点型 `DataList`，送货方式和付款方式是 `Select`，三者共用 `.fn-dropdown__menu`。新运单会自动聚焦到站并打开菜单，而额外点击活动页签或控件会让菜单失焦或再次切换关闭；接口虽然返回候选，DOM却可能没有保持可见。
6. **旧菜单壳和跨订单候选复用。** 页面会复用候选 DOM，也可能保留空菜单壳；仅以“菜单存在”或“节点是新出现的”判断，会漏选正确候选或误认旧菜单。

### 3. 解决方式

- 正式架构改为严格串行：`创建 → 读号 → 填写 → 12字段回读 → 保存成功 → 冷却 → 下一单`，并增加同号和数字未递增门禁。
- 启动时接管唯一空白草稿；只有在同号且逐个确认仍为空白时，才自动关闭重复页签，其他原有页签保持不动。
- 在页面真实执行环境同时观察 Fetch/XHR 与 `Response.json/text`，并保留 Performance 兜底和脱敏诊断时间线，使缓存 fetch 的 `errno=0` 能够正确归属订单。
- 保存成功后至少冷却 3 秒，再等待唯一创建入口稳定；若 DOM 被替换，只在完整运单号一致时重绑并重新核对普通字段。
- 对 DataList/Select 使用焦点敏感流程：优先复用新运单已经打开的菜单；必须主动打开时分阶段发送事件，每阶段检查，菜单出现即停止后续点击。
- `switchToTask()` 同时确认活动页签和唯一可见表单；目标已活动时零点击，避免关闭自动菜单。
- 候选选择同时校验当前字段目标文本、菜单可见性、距离、遮挡和唯一性；允许安全复用候选 DOM，但不接受旧空菜单壳。
- 所有异常都可导出不含个人资料的诊断 JSON，结合 HAR 区分“接口成功但前端未渲染”“响应捕获失败”和“网站刷新替换 DOM”。

### 4. 最终验证

- JavaScript 语法检查通过；
- 正式离线测试通过；
- 连续四单浏览器模拟通过，任意时刻最多一个保存请求，活动页签及到站均无多余点击；
- 连续六单真实网站验收通过，运单号 071～076 正确推进；
- 正式脚本版本保持 v1.0.13，不因仅更新验收文档而升版。

## TASK-20260814-013 界面重做为应用式面板并迁移交付目录

### 0. 目标与硬约束

目标：把调试风格的控制面板重做成普通用户能直接上手的应用式界面；正式交付物从 `outputs/<uuid>/` 迁到根目录 `script/`。

硬约束（用户明确要求）：**绝对不影响脚本处理逻辑。** 因此本轮先做了一次边界勘定，把文件切成"界面"与"引擎"两部分，再只动界面。

### 1. 变更范围

界面函数整体重写：`initializeUi`、`buildPanelHtml`、`renderSummary`、`taskActions`、`actionButton`、`stateLabel`、`taskExplanation`、`setStatus`。

新增 17 个界面辅助函数：`installPanelChrome`、`setWindowOpen`、`loadWindowPosition`、`applyWindowPosition`、`beginDrag`、`moveDrag`、`endDrag`、`pickFile`、`acceptDroppedFile`、`toggleDebugByTaps`、`uiConfirm`、`requestAuthorizedRun`、`taskTrouble`、`taskTone`、`plainIssue`、`plainStatus`、`renderShell`。

7 处 `window.confirm` 改为应用内弹窗（每处一行，语义等价）：`abandonTask`、`confirmAmbiguousSaved`、`confirmNotSaved`、`confirmManuallyClosed`、`retryOrderCreation`、`unlockDuplicates`、`discardRecoveredBatch`。后两个原为同步函数，现改为 `async`；`unlockDuplicates` 在 `window.__CMBatchSerial` 上导出，返回值由 `undefined` 变为 `Promise<undefined>`。

版本 `1.0.13` → `1.1.0`（`@version` 与 `SCRIPT_VERSION`）。`CHECKPOINT_VERSION` 保持 3，旧检查点仍可恢复。

文件迁移：`油猴批量串行自动填写保存.user.js` 与使用说明迁至 `script/`，新增 `script/README.md`；历史测试脚本与测试数据留在 `outputs/019fff22-6abf-7472-96f4-a0694f6fc144/`。

### 2. 界面设计要点

- **悬浮球 + 可拖动窗口。** 平时右下角只有一个 56px 的球，不遮挡车满满页面；点开是 720px 宽的窗口，按标题栏可拖动，位置存在 `cm-batch-ui-pos-v1`。球上有角标：运行中显示 `已完成/总数`，需要人工时变橙色显示待处理条数。
- **按批次阶段呈现。** 空闲 / 已导入待确认 / 导入有问题 / 运行中 / 需要人工 / 完成，同一时刻只渲染当前阶段，14 个平铺按钮收敛为每屏 1–3 个。
- **报错一律翻译。** 新增 `TROUBLE_BY_CODE`（覆盖全部 22 个 `errorCode`）、`TROUBLE_BY_STATE`（覆盖 21 个 state）、`STAGE_TEXT`、`FIELD_LABEL`、`STATUS_RULES`、`plainIssue`。查表顺序为 errorCode → state → MANUAL_STATES 兜底 → 硬编码兜底，保证任何未知状态都不会渲染空白。原始技术文本一字不改地保留在 `task.error` / `state.importErrors` 中，照常进入导出报告，界面上折叠进调试模式。
- **导入预检报错按列名翻译。** `第 3 行 quantity：必须为正整数` → `第 3 行「件数」：要填大于 0 的整数`；公式单元格列表里的英文列名也一并替换。
- **授权勾选框改为确认弹窗。** `#authorize` 复选框仍在 DOM 中（隐藏），门禁判断 `state.authorization` 完全没动。可见的「开始处理」「继续」是代理按钮，确认后写入 `#authorize.checked` 再 `.click()` 真正的 `#start` / `#resume`。**此处曾发现一个会导致功能损坏的坑：`resumeBatch` 同样以 `state.authorization` 为门禁，而刷新后该值恒为 `false`，若只给「开始」加弹窗，「继续」将永久失效。** 因此两个入口共用 `requestAuthorizedRun`。
- **隐藏调试开关。** 标题栏连点版本号 5 次切换，存在 `cm-batch-ui-debug-v1`。关闭时隐藏「导出诊断JSON」、技术详情、原始状态栏文案和"已挡下的意外保存"计数。以后要删除 JSON 报告，只需去掉该开关，`#diagnostic` 按钮保留在 DOM 中（`initializeUi` 无条件按 id 缓存，删除会抛错）。
- **拖拽导入。** 拖入的文件通过 `DataTransfer` 写进隐藏的 `#file.files` 再派发 `change`，因此 `handleFileSelection` 与 `resetForNewBatch` 里的 `file.value = ""` 完全不用改。

### 3. 必须保持的隐式契约（本轮记录，供后续维护）

1. `initializeUi` 按 16 个 id 无条件缓存元素，任何一个缺失都会抛错 —— 不用的元素只能隐藏，不能删。
2. 任务操作按钮必须带 `data-action` + `data-task`，`handlePanelAction` 依此分派；界面外壳按钮一律不带 `data-action`。
3. host 元素 id 必须是 `cm-batch-serial-production-host`，引擎有 13 处用它把面板排除在 DOM 扫描外。补充结论：`document.querySelectorAll` 不穿透 open shadow root，所以真正的不变量是"界面 DOM 绝不放到影子树之外"。
4. `resetForNewBatch` 直接写 `file.value` 与 `authorize.checked`，两者必须是真实的 input 元素。
5. `exportReportCsv` 在批次完成时被自动调用（非用户点击），签名不能改。
6. `sanitizeForReport` 夹在界面函数之间但属于脱敏逻辑，一行未动；界面上渲染任何原始文本都必须经过它。

### 4. 验证

- **函数级 diff（主证据）。** 按顶层函数切块比对改动前后：146 → 163 块，**128 个引擎函数逐字节零差异**，含全部拦截栈、`validateOrders`、`runScheduler`、`processTask`、下拉引擎、`verifyCompleteOrder`、`saveSingleTask`、`applySaveResult`、`handlePanelAction`、`persistCheckpoint`/`restoreCheckpoint`、台账、`switchToTask`、XLSX 解析、`sanitizeForReport`、三个报告函数与全部工具函数。有差异的 18 块中，3 块为切块工具把新增常量归入前一函数所致（逐行核对：0 行删除，仅新增），其余 15 块即上述界面函数与 7 处 confirm。
- **语法检查通过**（`deno check`，退出码 0）。
- **浏览器模拟通过。** 在本地静态页加载脚本：16 个 id 全部存在、`#filter` 四个 option 值不变、`window.__CMBatchSerial` 仍为 24 个键、`version` 为 `1.1.0`；逐一驱动 5 个阶段渲染正确；导入预检 15 类报错全部翻译且无 `ZIP`/`XML`/`errno`/英文列名泄漏；确认弹窗取消后 `state.authorization` 仍为 `false` 且 `batchStarted` 未变；调试模式开关双向生效。
- **拖动安全性。** 合成事件（`isTrusted === false`，即引擎 `dispatchUserLikeClick` 派发的那类）被拖动处理器拒绝，窗口位置不变；真实事件路径下位移精确、越界夹取正确（8px 边距）、位置持久化正常、点击标题栏按钮不会触发拖动。
- **尚未验证：实站回归。** 本轮没有在真实账号上执行保存。

### 5. 下次注意事项

1. **实站回归尚未做。** 上线前必须用 2 条虚构订单跑一次完整流程，确认：保存成功、12 字段回读通过、成功页签保留、批次完成自动导出 CSV、检查点被清除。
2. 若离线测试中有同步调用 `__CMBatchSerial.unlockDuplicates()` 的用例，需改为 `await`。
3. 新增任何界面元素时，遵守"`data-action` 只给任务按钮"这条约定，否则会被 `handlePanelAction` 误捕。
4. `renderSummary` 在每次 `transitionTask` 时被调用（每单约 8 次）。当前 100 条任务的渲染为单次模板拼接，未观察到卡顿；若日后表格再变复杂，需实测后考虑 rAF 合并。
5. 引擎侧新增 state 或 errorCode 时，记得同步补 `TROUBLE_BY_CODE` / `TROUBLE_BY_STATE` / `STAGE_TEXT`；漏补不会报错，会落到兜底文案。

---

## TASK-20260814-014：鲁棒性加固（断点续跑、关闭确认、慢网络超时放宽）

### 1. 任务目标

在不改变处理逻辑（状态机转移判定、保存门禁、拦截守卫、字段回读）的前提下提升 v1.1.0 的鲁棒性，并实现三项用户需求：浏览器/页面关闭后重开可点「继续」续跑未保存订单；运行中关闭页面弹出确认框；慢网络下放宽超时，解决"保存慢导致下一单找不到标签页"。

### 2. 只读检查结论（本轮起点）

通读全部 2498 行后确认：核心链路无会导致错误下单的缺陷，所有可疑路径均收敛到"停下来等人工"。剩余提升空间集中在四类：catch 内对非 Error 抛出物的属性访问会二次崩溃；检查点每次全量序列化 500 条诊断事件写 localStorage 的配额与卡顿风险；后台标签页定时器节流会拉长等待、误判超时；`baselineTabs` 强持有已分离 DOM。另有三项涉及判定边界的发现（`isOrderNumberAdvanced` 不等长数字直接放行、无 `r` 属性单元格 XLSX 解析失败、候选扫描全量 DOM 遍历），用户未批准修改，仅记录为已知边界。

### 3. 实现内容（v1.2.0）

**防御性加固（不改判定）：**

- `processTask`/`failTask`/`fillDirectTask`/`fillCustomTask`/`verifyCompleteOrder` 的 catch 内 `error.xxx` 全部改为 `error?.xxx`。
- `unlockDuplicates` 的 `saveLedger` 补 try；失败时报"解除防重复记录失败"并中止解锁。
- `persistCheckpoint` 写入失败时把诊断事件裁到最近 50 条重试一次，仍失败才 globalHalt（原有行为）；成功路径不变。
- `installSafetyGuards` 新增 `error`/`unhandledrejection` 监听，仅 `recordDiagnostic("unhandled_error")`。
- `MinimalZip.read`/`readDirectory` 将截断文件触发的 DataView RangeError 包装为中文"不是有效的 XLSX/ZIP 文件或文件已损坏"。

**环境稳健性：**

- `baselineTabs` Set→WeakSet（仅 `.has()` 用法，`WeakSet.has(null)` 返回 false 与 Set 一致，零行为差异）。
- 新增 `visibilitychange` 监听：记诊断事件；批次运行中转后台时状态栏警告保持前台。

**断点续跑：** 新增 `PRE_SAVE_STATES`（creating/waiting_create_entry/direct_filling/custom_pending/custom_filling/verifying/ready_to_save/mapping_failed/manual_pending/order_number_blocked/create_entry_blocked——均从未触发保存请求）。`restoreCheckpoint` 将其映射为 `pending` 并清空单号/字段/save/errorCode，`creationAttempted` 置 `true`，使重跑首单先走既有 `resolveExistingDraft` 页签预检，网站未清理的遗留草稿会被现有安全机制拦住。`saving`/`save_ambiguous` 仍进 `save_ambiguous`；`save_failed`/`decision_required`/旧 `interrupted_manual` 仍进 `interrupted_manual`（用户确认：已点过保存的一律人工核对，防重复下单）。`resumeBatch` 门禁与授权弹窗未动。恢复横幅与状态栏文案改为"没保存过的订单已重新排队，点「继续」接着做"。

**关闭确认：** 新增 `beforeunload` 监听，`state.running` 或存在未 settled 保存许可时 `preventDefault`；暂停/完成状态不拦（检查点已落盘）。

**慢网络超时（提为顶部命名常量）：** 保存许可 20s→60s、请求捕获 5s→10s、performance 兜底 1.5s→3s、创建入口/结果 15s→30s、页签切换 5s→10s、重绑 8s→15s、关闭观察 4s→8s、到站候选 2.4s/4.5s→4.8s/9s、下拉候选 1.8s/3.5s→3.6s/7s、选择确认 3.5s/3s→7s/6s、保存后冷却 `POST_SAVE_COOLDOWN_MS` 3s→6s。冷却×2 直接对应用户报告的症状：TASK-004（v1.0.6）记录的保存后约 2 秒异步 DOM 刷新窗口在慢网下变长，旧 3 秒冷却不够，下一单创建期间 DOM 被替换导致绑定失败。所有超时只推迟失败判定，成功路径条件满足即返回。节奏性 sleep（100–300ms）与 500ms 稳定判定未动。

版本 `1.1.0` → `1.2.0`；`CHECKPOINT_VERSION` 保持 3（快照结构未变，仅恢复映射变化，v2/v3 旧检查点仍可恢复）。

### 4. 验证

- `deno check` 通过；grep 确认旧超时魔数（20000/15000/8000/5000/4500/4000/3500/3000/2400/1800/1500）全部替换为常量、无遗漏。
- 浏览器模拟（本地静态页加载脚本 + 注入 v2 检查点）23/23 通过：7 类状态恢复映射正确；重排任务字段清空且 `creationAttempted=true`；首写检查点模拟 QuotaExceededError 后以 50 条事件重试成功且 `globalHalt` 为空、v2 旧键被删除；恢复状态栏白话文案正确（含重排/人工计数）；beforeunload 空闲不拦、`running` 时拦截；unhandledrejection 进入诊断；无 EOCD 与 EOCD 越界两类损坏文件均报中文错误；`window.__CMBatchSerial` 仍为 24 键。
- **离线测试通过，真实系统验证未做。**

### 5. 下次注意事项

1. **实站回归必做**：2 条虚构订单完整流程；另需专门验证断点续跑——第 1 单保存成功后、第 2 单填写中途关闭整个标签页，重开后确认第 2 单显示"排队等待"、点「继续」后先走页签预检再正常创建。
2. 慢网验证：可用 DevTools Network 节流（Slow 3G）跑一单，确认保存等待不再 20 秒误判为结果不确定。
3. `PRE_SAVE_STATES` 与恢复映射强耦合：引擎侧新增任何"保存前"state 时必须同步加入该集合，否则恢复后会落到 `interrupted_manual` 人工兜底（安全但体验退化）；新增"保存后"state 则绝不能加入。
4. 检查点降级重试只影响落盘副本；内存 `state.diagnostics` 仍保留 500 条上限，导出诊断 JSON 不受影响。
5. `baselineTabs` 已是 WeakSet，不可再对其做遍历或计数类扩展。
6. 三项已知边界（不等长运单号比较、无 `r` 属性 XLSX、候选全量扫描）为用户明确不改项，动它们前先取得确认。

---

## TASK-20260814-015：展开订单明细后窗口长出屏幕

### 1. 现象

用户报告：展开「订单明细」折叠区后，助手窗口的位置不会自适应改变，窗口底部长到屏幕外面，每次都必须手工把窗口拖回来才能继续看。

### 2. 根因

`applyWindowPosition(x, y)` 的边界夹取逻辑本身是正确的（`maxY = innerHeight - height - 8`，会把窗口拉回可视区），但它只在三个时机被调用：`setWindowOpen(true)` 打开窗口、`moveDrag` 拖动过程中、`window.resize` 浏览器窗口尺寸变化。**面板自身内容高度变化时没有任何调用点。**

叠加两个既有设计放大了后果：一是默认位置为 `Math.max(8, maxY - 24)`，即贴近屏幕底部；二是 `.win` 的 `max-height:86vh` 允许窗口长到接近整屏。于是展开明细时窗口只能从底部向下生长，直接越过可视区下边界。

实测数据（视口 1000px、12 条订单）：折叠时窗口 `top=531 / height=437 / bottom=968` 正常；展开后高度增至 860，而 `top` 仍为 531，底边落在 **1391px，超出视口 391px**。

### 3. 修正

新增 `reclampWindowPosition()`，内部复用既有 `applyWindowPosition(panel.pos?.x, panel.pos?.y)`，不重复实现夹取算法。两个触发点：

- `#detail` 的 `toggle` 事件 —— 直接对应用户报告的操作；
- `renderShell()` 末尾 —— 覆盖阶段切换、人工卡片弹出、运行中表格行数变化等其他会改变高度的路径（`renderSummary` 每次 `transitionTask` 都会走到，因此运行期间的内容增长也被兜住）。

两条约束：拖动过程中不干预（`panel.drag` 判空，避免与 `moveDrag` 抢位置）；自动避让**不**写入 `localStorage`（`endDrag` 才持久化），因此用户手工拖到的位置仍然是下次打开窗口时的首选，窗口变矮后不会被自动避让的坐标污染。

### 4. 曾尝试并否决的方案：ResizeObserver

初版用 `new ResizeObserver(reclamp).observe(el.win)`，理论上覆盖面最广（含浏览器缩放、文本换行等一切高度变化）。**但在 Browser 预览环境中该观察器一次都没有触发**：页面探针显示 `firedTotal: 0`，连 `observe()` 本应立即投递的初始回调都没有；同一时刻 `getBoundingClientRect()` 却能读到 437→860 的真实高度变化。原因是 ResizeObserver 的通知在事件循环的「更新渲染」步骤投递，而预览面板未显示时页面不合成帧。

真实可见标签页中它会正常工作，但**在本环境中无法验证**。据此改用 `toggle` + `renderShell` 组合：两者都不依赖渲染帧、可被测试直接驱动，覆盖范围与实际需求一致。结论记在此处，避免后续维护者再次绕回 ResizeObserver。

### 5. 验证

- `deno check` 通过。
- 浏览器模拟 13/13 通过（视口 1280×1000、12 条订单的已恢复批次）：
  - **对照组确认 bug 真实存在** —— 断言 `topBefore + heightAfter > innerHeight`，实测旧行为会超出底边 391px；
  - 修复后展开明细，窗口自动上移 `top 531 → 132`、`bottom=992` 完整落在视口内；
  - 折叠后仍完整可见，未被弹走；
  - 窗口停在上方且空间充足时展开**不移动**（`top 132 → 132`），确认只在越界时才介入；
  - 无横向溢出；阶段/内容变化后同样完整可见；`window.__CMBatchSerial` 仍为 24 键。
- v1.2.0 回归套件 22/23 通过，唯一 FAIL 是该套件写死的 `version === "1.2.0"` 断言过期，非行为回归。
- **离线测试通过，真实系统验证未做。**

### 6. 下次注意事项

1. 实站回归与 v1.2.0 一并做；顺带在真实页面确认展开明细后窗口自动上移、且拖到别处后不会被抢位置。
2. 若日后给面板新增会改变高度的区块，不必再加触发点 —— 走 `renderShell` 的自然会被覆盖；只有绕过 `renderSummary` 直接改 DOM 的路径才需要手动调 `reclampWindowPosition()`。
3. `reclampWindowPosition` 刻意不持久化位置。如果以后要改成持久化，先想清楚"窗口变高被迫上移"是否应该覆盖用户手工选定的位置。
4. 本环境（Browser 预览面板未显示时）不投递 ResizeObserver 回调、也无法截图，涉及渲染帧的特性一律无法验证，选型时避开。

---

## TASK-20260814-016：v1.2.1 实站回归验收

### 1. 结果

用户在真实车满满系统完成 v1.2.1 测试，确认**一切正常**。TASK-013 / 014 / 015 三条记录中的「待实站回归」状态就此解除。

本次验收覆盖 v1.1.0 → v1.2.1 累积的全部改动：应用式面板界面重做、断点续跑、运行中关闭确认、慢网络超时放宽（保存 60s / 保存后冷却 6s / 其余 ×2）、订单明细展开后的窗口自动避让，以及 catch 可空访问、检查点写入降级重试、损坏 XLSX 中文报错等防御性加固。

### 2. 记录边界（重要）

本条为**用户口头确认的整体通过结论**，未回传逐条订单的字段回读数、运单号递增序列或脱敏 CSV 报告。因此：

- 不得据此引用任何具体条数或通过率指标；本文件中形如「6/6 保存、72/72 字段」的精确数据只属于 TASK-012（v1.0.13）那次有报告归档的验收。
- 若后续需要 v1.2.x 的精确实站证据（例如排查回归、对外说明稳定性），应重新跑一轮并归档导出的脱敏报告，不要引用本条。
- 本条**不覆盖**未曾进入实站路径的分支：`errno=320` 人工继续、保存结果不确定的人工核对、台账写入失败重试、旧 v1.0.0 并行检查点阻断等，这些仍只有模拟层面的验证。

### 3. 下次注意事项

1. 脚本侧本轮收尾。后续若改动引擎，实站回归须重做，且建议归档脱敏 CSV，以便留下可引用的精确数据。
2. 三项已知边界仍未处理，且用户明确不改：`isOrderNumberAdvanced` 不等长数字比较、无 `r` 属性单元格的 XLSX、`chooseNewExactOption` 全量 DOM 扫描。动它们前先取得确认。
3. Android 端「开单助手」目前只是初步搭建（TASK 未单列），尚无实机验收记录；其识别、导出与并发策略均未经过真实设备验证。

---

## TASK-20260815-001：到站确认放宽——选中标志 + 非空回显即成功

### 1. 任务目标

修复 `BATCH_20260815_01` 首单即阻塞的问题：用户明确提出鲁棒性原则——「只要脚本输入了文字，并成功在下拉菜单选择了，其实就是填写成功」。

### 2. 故障现象与根因

脱敏报告显示 TC_001（到站「杭州」）`destination_text` 报「候选点击后网站未确认」→ `mapping_failed` → `failTask` 熔断暂停整批，TC_002~012 全部 `pending`（不是各自有故障，而是根本没轮到）。

用户实看页面：脚本输入「杭州」后**成功点中了下拉里的「杭州市」，网站已选中**。根因是 `fillScopedAutocomplete` 点击后的确认条件要求回显与输入串**逐字相等**（`readControl(control) === value`），而网站把名字补全成标准行政区划后回显「杭州市」，严格相等永不成立，7 秒（`DATALIST_CONFIRM_TIMEOUT_MS`）超时误判失败。内置站点表里的站（通海→开跑前归一成「通海县」）查询串与回显一致，所以从不触发；只有表外到站原文直输才会踩中。候选能被点中是因为下拉项的高亮片段「杭州」恰好等于查询串，精确匹配命中后点击的是整个「杭州市」选项。

### 3. 修正

两处严格相等统一放宽为「网站自己的选中标志 `data-is-select === "1"` 且回显非空」：

- `fillScopedAutocomplete` 点击候选后的 `waitFor` 确认；
- `chooseNewExactOption` 轮询中的自动确认早退分支（`candidate_auto_confirmed`）。该分支若保留严格相等，网站自动确认成「杭州市」后脚本仍会继续找候选直至超时，误报「未找到唯一候选」。

刻意不动的安全边界：候选点选前的逐字精确匹配与「多候选歧义即停」不变；回显与输入完全无关时仍由 `fillCustomFields` 的 `includes` 回读校验（「选择后回读不一致」）兜底；`verifyDestinationKeyAttribute` 的 `xzqh_*` 编码交叉校验不变。

### 4. 验证

- `deno check` 通过；`destination.test.mjs`、`payment-schema.test.mjs` 全绿（版本断言同步改为 1.4.1）。
- **离线测试通过，真实系统验证未做。**「杭州→杭州市」这条真实路径需实站重跑确认。

### 5. 下次注意事项

1. 实站回归：重跑 `BATCH_20260815_01`（或等效含表外到站的表格），确认「输入简称、选中标准名」不再停批，并归档脱敏 CSV。
2. 测试表格 `测试开单数据_12条.xlsx` 的到站（杭州、广州……）都不在内置站点表里，属于表外原文直输路径；若业务上确实只发通海/玉溪，正式数据仍应写标准名或走 v1.2 编码列。
3. 放宽后脚本采信的是「网站确认了本次点选」，若网站未来出现「点 A 选项确认成 B 站」的怪异行为，兜底只剩 `includes` 回读与 `xzqh_*` 编码比对两道，排查时先想这一点。
4. **用户 2026-08-15 确认：多候选歧义保持「即停」不动**（`BATCH_20260815_01` 的 TC_005「西安」匹配到西安市/西安区×2 共 3 个候选触发停批，用户选择不放宽）。表外到站的正确写法是网站标准全名（西安市、长沙市），全名查询的候选精确匹配唯一，不会触发歧义；简称（西安、长沙）遇到同名区县时就会停批等人工，这是设计行为不是故障。

---

## TASK-20260815-002：明细滚动修复 + 人工卡片显示字段期望值

### 1. 任务目标

用户反馈两个面板 UI 问题：① 订单明细展开后无法滚动，看不到下面的订单；② 人工处理卡片提示「对不上的项目」只列字段名，没说应该填什么。

### 2. 根因

- **滚动**：`.win` 是 `max-height:86vh; overflow:hidden` 的 flex 列容器，`.scroll` 设了 `flex:1; overflow:auto` 但缺 `min-height:0`。flex 子项默认 `min-height:auto`，无法收缩到内容高度以下，于是 `.scroll` 被内容撑高、永远不产生滚动条，超出的明细行直接被窗口的 `overflow:hidden` 裁掉——表现为「无法滚动」。TASK-20260814-015 的实测数据（展开明细后窗口高度顶到 860px = 86vh）与此吻合：窗口被裁到上限，但内部内容仍超出。
- **期望值**：`renderSummary` 人工卡片的 `missing` 只取 `FIELD_LABEL`，没带 `task.order` 里的期望值。

### 3. 修正

- `.scroll` 增加 `min-height:0`（一行 CSS，滚动机制本身不变）。
- `missing` 改为 `字段名（应为：期望值）`；期望值取自 `task.order[key]`，枚举字段（付款方式等）走 `mapping.display` 显示名；值为空的字段只显示字段名。整体仍过 `escapeHtml`，无注入面。

### 4. 验证

- `deno check` 通过；`destination.test.mjs`、`payment-schema.test.mjs` 全绿（版本断言同步 1.4.2）。
- **离线测试通过，真实系统验证未做。**两处都是纯 UI 层改动，引擎零改动。

### 5. 顺带记录的实站证据（归 TASK-20260815-001）

v1.4.1 实站重跑 `BATCH_20260815_01`：TC_001~004（杭州/广州/武汉/成都）4/4 保存成功、字段全部 verified、HTTP 200 errno=0——「输入简称、选中网站标准名」路径实站验证通过。TC_005（西安）触发「候选出现 3 个新选项」歧义即停（西安市 + 两个西安区），用户确认**保持歧义即停不放宽**；表外到站规范写法是标准全名。

### 6. 下次注意事项

1. 实站回归时顺带确认：展开 12 条订单明细后面板内部可滚动；制造一单 mapping_failed 看人工卡片是否显示「应为：…」。
2. flex 滚动容器必须配 `min-height:0`——以后给面板加新的滚动区（如诊断导出预览）时直接照写，不要再踩。
3. TC_005 西安、TC_008 长沙这类简称撞同名区县的停批是设计行为；若用户以后想少停批，方向是把表格到站写成标准全名，而不是放宽歧义保护（用户已明确）。

---

## TASK-20260815-003：订单明细表格内部滚动

### 1. 任务目标

v1.4.2 修的是整个面板内容的滚动（`.scroll` 补 `min-height:0`），用户反馈明细框本身仍没有内部滚动——要的是明细区域限高、表格在明细框内自己滚。

### 2. 修正

明细块内的 `.tablewrap` 由 `overflow-x:auto` 改为 `overflow:auto; max-height:320px`。表格行多时明细框内部出滚动条；筛选下拉、统计行、导出按钮留在框内上部不动；明细也不会再把窗口撑高（v1.4.2 的 `min-height:0` 保留，作为外层兜底）。

### 3. 验证

- `deno check` 通过；`destination.test.mjs`、`payment-schema.test.mjs` 全绿（版本断言同步 1.4.3）。
- **离线测试通过，真实系统验证未做。**纯 CSS 改动，引擎零改动。

### 4. 下次注意事项

1. 实站确认：12 条订单展开明细后，表格在明细框内滚动、窗口总高不再被撑到 86vh 上限。
2. 沟通教训：用户说「明细无法滚动」时指的是**明细框内部滚动**，v1.4.2 修成了外层容器滚动。涉及「滚动」的需求先确认滚动的主体是谁。

---

## TASK-20260815-004：v1.5.0 保存后创建门控信号化（废弃固定 6s 冷却）

### 1. 任务目标

固定 6s 冷却（`POST_SAVE_COOLDOWN_MS`）是经验值盲猜：v1.0.6 时代 3s 在慢网翻过车才改成 6s，但没有证据表明 6s 在更差网络下依然够，快网下又每单白等约 3.5s。目标：用页面自身信号替代盲等，慢网自适应顺延。

### 2. 信号依据（先探针、后动手）

- 10 个历史 HAR 复盘：每次保存成功（coHandle errno=0）后页面自身必发 `POST /api/Order/Order/oinfo/`，延迟 2.13~2.18s；唯一一次 errno=320（未保存成功）则不发。
- 实站探针（`script/保存后创建时机探针.user.js`，只读观察）跑 12 单：oinfo 12/12 命中，延迟 2171~2289ms；oinfo 前后 ±150ms 是保存后异步 DOM 刷新窗口；oinfo 落定后再约 500ms 页面 DOM 进入持续安静（+2.8s 起连续 3.9s 零变更）；旧 6s 冷却实际 +6.7s 才点创建。
- 结论：「oinfo 响应 errno=0 + DOM 连续 500ms 无结构变更」作为创建门控，每个条件都对应实测机制；不存在单一"绝对安全"信号，网站不发布此类事件。

### 3. 修正

- `waitForPostSaveCooldown` → `waitForPostSaveReady`：oinfo 信号已收到且 DOM 安静 500ms 即放行（实测约 3s，每单省约 3.5s）；最长等 `POST_SAVE_SIGNAL_TIMEOUT_MS = 10000`，超时放行走 `timeout_fallback` 并写诊断 `post_save_ready`（不阻断批次，信号失效可从事后诊断发现）。
- 页面观察者新增 oinfo 匹配（仅响应侧：Response.json/text 与 XHR loadend，事件带 `signal: "oinfo"`）；oinfo 不发 request 事件、不进保存许可归因，coHandle 保存链路字节级不变。
- 新增常驻 MutationObserver（childList+subtree，过滤面板自身）只更新 `state.lastDomMutationAt`，供安静判定；信号时间戳为瞬态，不进检查点（刷新恢复后自然走超时路径，行为等同旧版）。
- 500ms 创建按钮稳定检测等下游校验原样保留，作为独立最后一道门。
- 探针脚本与分析脚本（`.codex-work/analyze_har.py`、`analyze_probe.py`）留库作后续验证工具。

### 4. 验证

- `deno check` 通过；`destination.test.mjs`、`payment-schema.test.mjs` 全绿（版本断言同步 1.5.0；该断言在 v1.4.4 时漏同步，本次一并修正）。
- **实站验证通过**（用户确认）：创建等待不再是固定 6s，而是随网络状况动态调整的信号门控，批次正常完成。

### 5. 下次注意事项

1. 实站回归若发现 `post_save_ready` 持续走 `timeout_fallback`，说明网站改版 oinfo 信号消失，需重新找信号（探针仍在库里，直接复用）。
2. 门控只缩短/顺延等待，不放宽任何下游校验；出现创建异常时先查诊断时间线里 `post_save_ready` 与 `create_entry_stable` 的间隔。
