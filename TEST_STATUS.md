# 测试状态记录

## 2026-08-14 — v1.2.1：实站回归通过（真实系统验证）

- 用户在真实车满满系统完成 v1.2.1 实站测试，确认**一切正常**，本轮任务收尾。
- 覆盖范围即 v1.1.0 / v1.2.0 / v1.2.1 三个版本累积的全部改动：界面重做、断点续跑、运行中关闭确认、慢网络超时放宽、窗口定位修正及各项防御性加固。
- 由此解除此前三条记录中的「尚未验证：真实网站回归」状态。
- **记录边界**：本次为用户口头确认的整体通过结论，未回传逐条订单的字段回读数、运单号序列或脱敏 CSV 报告。因此不得据此引用具体条数或通过率指标；如需精确数据，应另行导出报告归档（可参考 v1.0.13 那次的记录方式）。

## 2026-08-14 — v1.2.1：展开订单明细后窗口长出屏幕（界面定位修正）

- **现象**：展开「订单明细」后助手窗口不会自适应位置，底部长出屏幕外，每次都要手工把窗口拖回来。
- **根因**：`applyWindowPosition` 的夹取逻辑本身正确，但只在「打开窗口 / 拖动 / 浏览器 resize」时被调用，**面板自身高度变化时无人触发**；而默认位置是贴近屏幕底部的 `maxY - 24`，展开后窗口只能向下长出可视区。
- **修正**：新增 `reclampWindowPosition()`，复用既有 `applyWindowPosition` 重新夹取。挂两个触发点：`#detail` 的 `toggle` 事件（对应用户报告的操作）与 `renderShell` 末尾（覆盖阶段切换、人工卡片弹出、表格行数变化）。拖动过程中不干预（`panel.drag` 判空），自动避让不写入 `localStorage`，用户手工拖到的位置仍是下次打开的首选。
- **曾尝试并否决 ResizeObserver**：在 Browser 预览环境中该观察器一次都不触发（连 `observe()` 的初始回调都没有，其通知依赖渲染帧而预览环境不合成帧），无法验证，故改用可验证的 `toggle` + `renderShell` 组合，覆盖范围相同。
- `deno check` 通过。浏览器模拟 13/13 通过（视口 1280×1000，12 条订单）：**对照组确认 bug 复现**——旧行为下窗口底边会落在 1391px、超出 1000px 视口 391px；修复后窗口自动上移 `top 531→132`、`bottom=992` 完整可见；折叠后仍可见；窗口停在上方且空间充足时展开不移动位置（`top 132→132`）；无横向溢出；阶段/内容变化后同样可见；`window.__CMBatchSerial` 仍为 24 键。
- v1.2.0 回归套件 22/23 通过，唯一 FAIL 为该套件写死的 `version === "1.2.0"` 断言过期，非行为回归。
- **实站回归**：已于同日通过，见本文件顶部记录。

## 2026-08-14 — v1.2.0：鲁棒性加固（离线与浏览器模拟通过，实站回归待做）

- **改动性质**：不改变任何状态机转移判定、保存门禁、拦截守卫和字段回读逻辑；只加固异常兜底、存储降级、超时参数和检查点恢复映射。
- **本次改动的引擎函数清单**（与 v1.1.0 相比）：常量区（新增 15 个超时常量、`PRE_SAVE_STATES`、`POST_SAVE_COOLDOWN_MS` 3000→6000）、`state` 初始值与 `startBatch`/`resetForNewBatch`（`baselineTabs` Set→WeakSet，仅 `.has()` 用法，零行为差异）、`installSafetyGuards`（新增 error/unhandledrejection/visibilitychange/beforeunload 四个只观测或只提示的监听）、`installPerformanceObserver`/`armSavePermit`/`saveSingleTask`/`createAndBindTask`/`inspectCreationTabs`/`activateInspectedDraft`/`closeVerifiedEmptyDraft`/`closeOwnedTab`/`switchToTask`/`rebindTaskContext`/`fillScopedAutocomplete`/`chooseScopedValue`（超时魔数替换为常量并按"保存60s/其余×2"放宽）、`processTask`/`failTask`/`fillDirectTask`/`fillCustomTask`/`verifyCompleteOrder`（catch 内 `error?.xxx` 可空访问）、`persistCheckpoint`（写入失败裁剪诊断事件至 50 条重试一次，仍失败才 globalHalt）、`restoreCheckpoint`（保存前状态重排为 pending）、`unlockDuplicates`（`saveLedger` 补 try）、`MinimalZip.read`/`readDirectory`（RangeError 包装为中文损坏提示）。界面侧仅改恢复横幅一句文案和 `STATUS_RULES` 4 条。
- **断点续跑映射**：从未触发过保存请求的状态（creating/waiting_create_entry/direct_filling/custom_pending/custom_filling/verifying/ready_to_save/mapping_failed/manual_pending/order_number_blocked/create_entry_blocked）恢复为 `pending` 并清空单号/字段/save，`creationAttempted` 置 `true` 使重跑先走既有 `resolveExistingDraft` 页签预检；`saving`/`save_ambiguous` 仍进 `save_ambiguous`，`save_failed`/`decision_required`/旧 `interrupted_manual` 仍进 `interrupted_manual`（防重复下单，用户确认）。`resumeBatch` 门禁未动。
- **超时放宽表**：保存许可 20s→60s；请求捕获 5s→10s；performance 兜底 1.5s→3s；创建入口/创建结果 15s→30s；页签切换 5s→10s；重绑 8s→15s；关闭观察 4s→8s；到站候选两轮 2.4s/4.5s→4.8s/9s；下拉候选两轮 1.8s/3.5s→3.6s/7s；选择确认 3.5s/3s→7s/6s；保存后冷却 3s→6s（针对慢网下保存后异步刷新窗口变长导致下一单绑定失败）。所有超时只推迟失败判定。
- `deno check` 语法检查通过；grep 确认旧超时魔数全部替换、无遗漏。
- 浏览器模拟 23/23 通过：v2 检查点恢复后 7 类状态映射正确（3 条重排、2 条 interrupted_manual、1 条 save_ambiguous、1 条 saved）；重排任务字段清空且 `creationAttempted=true`；检查点首写模拟配额错误后以 50 条事件重试成功、不触发 globalHalt；恢复状态栏白话文案正确；beforeunload 空闲不拦/运行中拦截；unhandledrejection 进入诊断事件；无 EOCD 与 EOCD 指向越界两类损坏文件均报中文错误；`window.__CMBatchSerial` 仍为 24 键。
- **明确未做**（用户未批准）：`isOrderNumberAdvanced` 不等长数字比较未收紧；不支持无 `r` 属性单元格的 XLSX；`chooseNewExactOption` 全量 DOM 扫描范围未改。
- **实站回归**：已于同日通过，见本文件顶部记录。

## 2026-08-14 — v1.1.0：界面重做，引擎零改动（离线与浏览器模拟通过，实站回归待做）

- **引擎未改动的证据**：按顶层函数切块比对 v1.0.13 与 v1.1.0，128 个引擎函数逐字节零差异，包含全部拦截栈、`validateOrders`、`runScheduler`、`processTask`、下拉引擎、`verifyCompleteOrder`、`saveSingleTask`、`applySaveResult`、`handlePanelAction`、检查点与台账、`switchToTask`、XLSX 解析、`sanitizeForReport` 及三个报告构建函数。
- 有差异的仅 8 个界面函数、7 处 `window.confirm`（每处一行，改为应用内弹窗）和版本号；另有 17 个新增界面辅助函数。
- `deno check` 语法检查通过。
- 浏览器模拟：16 个缓存元素 id 全部存在；`#filter` 四个 option 值不变；`window.__CMBatchSerial` 仍为 24 个键、`version` 为 `1.1.0`；5 个界面阶段逐一驱动渲染正确。
- 文案脱密检查：15 类导入预检报错全部翻译为中文列名说明，界面上未出现 `ZIP`、`XML`、`errno`、`回读`、`检查点`、`台账` 或英文列名。原始文本仍保留在 `task.error` / `state.importErrors` 并原样进入导出报告。
- 授权门禁检查：确认弹窗点「取消」后 `state.authorization` 仍为 `false`、`batchStarted` 未变；`startBatch` 与 `resumeBatch` 的门禁判断均未改动。
- 拖动安全检查：`isTrusted === false` 的合成事件（即引擎 `dispatchUserLikeClick` 派发的那类）被拖动处理器拒绝，窗口不会被网站或引擎误拖；真实事件下位移精确、越界夹取正确、位置持久化正常。
- **实站回归**：已于同日随 v1.2.1 一并通过，见本文件顶部记录。

## 2026-08-14 — v1.0.13：正式流程全部功能实站验收成功

- 验收报告：`车满满批量执行脱敏报告-TEST-20260814-2026-08-14T14-44-11-355Z.csv`；原始报告仅用于本地核对，不提交 Git。
- TEST-0001～TEST-0006 共 6 条订单全部进入 `saved`，没有失败、结果不确定或人工待处理记录。
- 每条订单 12/12 字段均为 `verified`，合计 72/72 字段回读通过。
- 6 次保存响应均为 HTTP 200、`errno=0`，捕获通道均为 `response.text`。
- 运单号后缀依次为 071、072、073、074、075、076，证明“上一单明确保存后再创建下一单”的串行门禁在连续批次中正确推进号码。
- 6 条身份指纹均唯一，脱敏报告未包含姓名、手机号或完整运单号；正式流程全部功能测试成功。

## 2026-08-14 — v1.0.13：已活动的新运单页签不再重复点击

- 用户提出另一关键时序：新运单创建后可能需要切换到新页签才会自动聚焦到站并显示菜单。
- 代码核对发现，创建绑定依赖“活动新页签或唯一可见新表单”，但进入下拉字段前的 `switchToTask()` 仍无条件点击任务页签；即便新页签早已活动，这次重复点击也可能让到站失焦、关闭自动菜单。
- v1.0.13在创建绑定后立即调用上下文确认；`switchToTask()`只有在目标页签或表单尚未活动时才点击，已经活动时记录 `task_switch: already_active=true, click_dispatched=false`。
- `new_form_bound`诊断新增页签活动、表单活动、当前焦点字段和可见菜单数，可直接区分“DOM已创建但未切换”与“已切换并自动聚焦”。
- 连续四单模拟要求每次创建后新页签已活动、到站已聚焦、菜单已打开；所有任务切换点击、活动页签重复点击和到站额外点击均严格为0，039–042全部完成12字段并保存。
- JavaScript语法、离线测试和完整浏览器模拟通过；完整模拟约146秒。

## 2026-08-14 — v1.0.12：保留网站自动打开的到站菜单

- 用户确认真实行为：创建任何新运单后，网站都会默认聚焦到站输入框并自动显示下拉菜单；v1.0.11随后又对同一控件发送完整点击，肉眼可见菜单短暂出现后关闭，再进入逐字重试。
- v1.0.11诊断与现象一致：第二单各次打开尝试结束时菜单为0，但接口正常；这说明菜单在同步事件序列中途出现，又被后续事件切换关闭。
- v1.0.12先检查活动元素是否为当前到站控件且菜单可见；满足时记录 `already_open_for_active_control` 并直接输入，不发送任何额外点击。
- 必须主动打开时改为分阶段事件：focus、pointerdown、mousedown、release、click；每阶段等待并检查，菜单出现且释放后仍可见便停止，不再发送click。送货/付款还要求菜单中出现当前目标文本，避免遗留空菜单壳误判。
- 连续四单模拟为每个新表单自动聚焦到站并预先显示菜单，同时把“额外点击到站”设为关闭菜单；最终039–042四单全部通过12字段并保存，到站额外点击严格为0。
- JavaScript语法、离线测试和完整浏览器模拟通过；完整模拟约142秒。

## 2026-08-14 — v1.0.11：按DataList/Select主动呼出菜单及连续四单

- 用户提供真实控件DOM：到站为 `data-field-type="DataList"`，送货方式为 `data-field-type="Select"`。下拉菜单可以由控件交互主动呼出，不能把“诊断未看到菜单DOM”简单等同于网站不能创建菜单。
- HAR没有包含页面组件JavaScript正文，无法直接调用网站内部实例方法；v1.0.11改为模拟完整用户打开序列：聚焦、pointer/mouse over/down/up/click。鼠标后仍无菜单时发送一次 `ArrowDown`，不发送Enter。
- 到站在查询响应后再次检查并主动打开；两个Select每轮选择前主动打开。诊断新增 `dropdown_open_attempt`，区分鼠标打开、鼠标加方向键打开和已经可见。
- 新增连续四单浏览器场景：运单号039、040、041、042依次创建并保存；第2、3、4单的到站、送货方式、付款方式首次渲染均被故意丢弃，要求全部经过受控恢复并通过12字段回读。
- JavaScript语法、离线测试和完整浏览器模拟通过；完整模拟约140秒，四单场景始终最多一个保存请求，四个运单号严格递增。

## 2026-08-14 — v1.0.10：相同到站逐字重触发与枚举焦点重试

- v1.0.9诊断确认TEST-0001的到站、送货方式、付款方式均已选择并保存；TEST-0002只执行到到站便停止，因此页面上送货方式和付款方式为空属于“尚未执行”，不是两个独立字段已经失败。
- TEST-0002已成功创建尾号069。两轮目标到站查询均为HTTP 200、`errno=0`、10条POI和一个精确候选；诊断分别观测到1次和2次已完成资源请求，但 `.fn-dropdown__menu` 始终为0。
- v1.0.10保留首轮完整值输入；首轮未渲染时，第二轮从空值开始逐字写入并发送对应键盘/输入事件，用不同前缀打破网站组件对上一订单相同完整查询的状态复用。
- 如果网站已自行把控件标记为 `data-is-select=1` 且回读值精确一致，脚本记录 `candidate_auto_confirmed` 并接受，无需强求菜单DOM。
- 送货方式和付款方式增加独立 `choice_retry`：首次点击1.8秒无菜单时失焦180ms，再聚焦点击一次；仍无唯一候选才停止。
- 语法、离线测试及完整浏览器模拟通过。模拟同时丢弃第二单到站、送货方式、付款方式的首次菜单渲染，验证三者只重试一次后恢复。

## 2026-08-14 — v1.0.9：识别焦点型下拉浮层

- 用户提供真实DOM：到站候选位于 `<div class="fn-dropdown__menu">`，该浮层在输入控件失焦时立即消失，因此无法通过切换到开发者工具稳定复制子节点。
- 该结构进一步确认到站流程不能发送提前的 `change`、不能主动 `blur`；v1.0.8的输入顺序保持不变。
- v1.0.9 将可见 `.fn-dropdown__menu` 作为候选首选边界；浮层中存在精确文本时，不再让页面其他区域的同名文字参与歧义判断。
- 候选选择、重试和超时诊断新增浮层总数、可见数及浮层内精确候选数，仍不导出候选文本或HTML。
- 语法、离线测试及完整浏览器模拟通过；模拟候选统一使用真实浮层类，并覆盖首轮不渲染、二次复用节点及失焦敏感选择路径。

## 2026-08-14 — v1.0.8：联想接口成功但候选未渲染

- 对齐 v1.0.7 诊断与 `自动流程1.0.7.har`：TEST-0001 保存成功；TEST-0002 已创建新运单、号码由尾号067推进到068，失败仍发生在到站字段。
- 第二单 `/api/Basic/Nsug/sug/` 于约82ms内返回HTTP 200、`errno=0`、10条POI，且含一个精确候选；诊断随后持续4.5秒仍为 `exact=0/new_targets=0/reusable_targets=0`。因此本次不是后端、创建或旧节点过滤问题，而是网站联想组件收到响应后没有产生可见候选DOM。
- v1.0.8 不再用普通字段的“input+change”连发方式填写到站。它会聚焦并点击控件、清空旧选择标记、发送清空输入，再发送目标输入；首轮仍无可见候选时，只完整重置并重试一次。
- 诊断新增 `candidate_retry`，并在重试/最终超时事件记录同阶段联想资源请求数量、完成数量、最大耗时和浏览器可提供的HTTP状态码；不记录到站文本、请求体或响应正文。
- JavaScript语法、离线正式测试及完整浏览器模拟通过。模拟明确覆盖“第二单首轮查询完成但不渲染，第二轮才复用候选节点”的情形，并验证只发生一次重试。

## 2026-08-14 — v1.0.7：相同到站的候选DOM复用

- v1.0.6诊断确认第一单保存成功，第二单完成3秒冷却、创建入口稳定、单次创建、表单绑定和运单号递增；新号码尾号067，未再发生DOM断开。
- TEST-0002失败点为 `destination_text`：页面报告未找到唯一候选。
- `自动流程1.0.6.har` 显示第二单 `/api/Basic/Nsug/sug/` 返回HTTP 200、`errno=0`、10条POI；请求搜索文本在响应字符串中存在，证明后端候选正常。
- 两单搜索文本哈希相同。网站复用上一单候选DOM节点，而旧算法排除了输入前已经可见的所有节点，因此接口成功但DOM扫描结果为空。
- v1.0.7继续优先选择新出现的唯一节点；没有新节点时，只接受靠近当前控件、实际位于页面顶层且唯一的复用候选。两个以上仍停止。
- 诊断新增 `candidate_selected/candidate_ambiguous/candidate_timeout`，记录字段、选择模式、候选数量和搜索值哈希，不记录实际到站文本。
- 离线测试和完整浏览器模拟通过；模拟覆盖同一个候选节点跨订单保持可见并被再次复用，诊断确认选择模式为 `reused_dom`。

## 2026-08-14 — v1.0.6：保存后异步刷新与表单DOM重绑

- 分析 `自动流程1.0.5.har` 和用户导出的诊断JSON：TEST-0001通过 `response.text` 捕获 HTTP 200、`errno=0` 并正确保存尾号065。
- 保存响应约0.53秒后，脚本成功调用 `/api/Order/Order/co`；返回的新空白订单运单号尾号为066，证明下一单已经正确创建且单号推进。
- 随后网站在约2秒后调用 `/oinfo` 和订单列表刷新并替换内部DOM；脚本仍持有旧表单根引用，因此TEST-0002在进入自定义字段阶段报“订单页签或表单已从页面移除”。
- v1.0.6 保存成功后至少等待3秒，再进行创建入口500ms稳定检测，避开已观测到的异步刷新窗口。
- 新增同任务上下文重绑：活动表单重新读取的完整运单号必须与任务原号码完全一致，稳定500ms且无未完成任务冲突才更新页签/根引用；重绑后重新核对并补写普通字段。
- 诊断JSON新增创建开始、冷却、入口稳定、创建点击、结果出现、新表单绑定、运单号捕获及上下文重绑事件。
- 离线语法/逻辑测试通过；浏览器模拟覆盖保存后1.8秒刷新、3秒冷却后创建，以及同运单号表单根替换后的自动重绑。完整串行回归通过。

## 2026-08-14 — v1.0.5：缓存 fetch 响应捕获与诊断导出

- 分析 `Har/自动流程.har`：保存请求为 fetch，`/api/Order/Order/coHandle` 返回 HTTP 200、JSON、`errno=0`、`errmsg=保存成功`，耗时约117ms。
- HAR调用栈显示请求由 `common...js/orderEditor...js` 的模块内部请求函数发起；油猴脚本触发点击，但重新包装当前 `window.fetch` 无法影响网站已经缓存的函数引用。
- 新增页面真实执行环境观察器：除 Fetch/XHR 外，包装 `Response.json()` 和 `Response.text()`；网站读取缓存 fetch 的响应时，脚本可取得解析结果并按 `errno` 分类。
- Performance Resource 不再立即判定结果不确定，而是等待1.5秒，为页面响应正文观察器留出完成时间；仍无正文才进入人工核对。
- 面板新增“导出诊断JSON”，记录保存许可、点击、网络捕获层、响应结构元数据、Performance时序和最终分类；不记录请求体、Cookie、Token、原始响应、姓名、手机号、完整地址或完整运单号。
- 离线语法/逻辑测试通过。浏览器模拟通过普通 fetch 和网站预先缓存 fetch 两条路径；缓存路径第一单正确识别 `errno=0`，第二单正确识别 `errno=320`，诊断脱敏检查通过。

## 2026-08-14 — v1.0.4：成功页签保留与稳定创建入口

- 根据 `Har/完整流程.har` 校正下一单门禁：保存 `/coHandle` 于约136ms内返回 HTTP 200、`errno=0`，网站约2秒后才进入下一次开单流程；旧版立即查找创建按钮，存在页面尚未稳定时漏找的窗口。
- 成功订单不再自动关闭内部页签；页签标记为本批次已完成并保留，且不会被后续空白草稿扫描误判。
- “创建运单”优先按真实结构 `i.fn-icon.fn-icon-file-add` 定位所属链接，再以精确文字回退；要求唯一入口连续稳定500ms，最长等待15秒。
- 创建入口只单击一次；若单击后未观察到新增页签/表单，则转人工处理，不自动重复点击。
- v1.0.3 检查点中的 `saved_pending_close` 自动迁移为 `saved + retained`，并写入v3检查点。
- 离线语法/逻辑测试通过。浏览器模拟通过：保存后入口隐藏1.2秒再出现、稳定后创建040、成功页签保留、同号门禁、`errno=320`、重复入口阻断、现有空白页签复用、同号空白草稿清理、旧检查点迁移及未捕获响应的结果不确定分支。

## 2026-08-14 — v1.0.3：保存请求观察加固

- 修正网站实际保存成功、脚本却在5秒后标记“保存失败”的危险分类。
- 保存前重新包裹当前 Fetch/XHR，处理网站脚本在页面加载后替换网络实现的情况。
- 增加 Performance Resource 兜底观察；只能看到请求、无法读取响应时进入“结果不确定”。
- 点击保存后没有捕获到请求或响应时，也统一进入“结果不确定”，禁止直接重试或放弃为未保存。
- 页签已经消失时不再提供“放弃本单”按钮，必须查询订单列表并确认已保存或未生成。

## 2026-08-14 — v1.0.2：现有空白页签复用与同号清理

- 修正首次运行总是额外创建订单的问题：唯一空白“创建运单”页签现在直接绑定第一条 Excel 记录。
- 启动检查所有“创建运单”页签；多个页签只有在运单号相同且逐个重新确认仍为空白时，才自动关闭重复项并保留一个。
- 非空页签、不同运单号或无法确认表单/号码时均停止，不自动关闭。
- 增加真实关闭控件 `.fn-icon-close`，并允许批次关闭已明确接管的原有空白页签。
- 运单号读取增加活动表单外层可见字段兜底，解决运单号控件不在12字段根容器内时无法读取的问题。
- 浏览器模拟已覆盖：Home无草稿、唯一现有草稿复用、两个039空草稿自动清理并复用、随后保存后创建040。

## 2026-08-14 — 正式批量串行脚本 v1.0.1：开发完成

- 实站确认上一订单未保存时继续创建会复用相同运单号，v1.0.0 双并行正式路线因此撤销。
- 正式调度改为严格逐条：创建、读取运单号、填写、12字段回读、保存成功、关闭页签后，才创建下一单。
- 新增运单号读取、规范化和递增门禁；同号、同长度数字未递增或无法唯一读取时均在填写前暂停。
- 保存许可改为单实例；任意时刻最多一个未完成订单和一个保存请求。
- `errno=320` 只允许继续或放弃；填写/保存失败必须解决或放弃，成功页签关闭失败立即暂停。
- v1.0.0 并行检查点禁止自动恢复；防重复台账继续兼容旧记录。
- 脱敏报告新增运单号后缀及哈希，不输出完整运单号。
- JavaScript 语法及离线逻辑测试通过；浏览器模拟覆盖 039 保存并关闭后创建 040、单请求、`errno=320` 阻断及人工继续、原有页签保护和检查点恢复。

## 2026-08-14 — 正式批量并行脚本 v1.0.0：开发完成

- 新正式脚本支持 1–100 条 Excel v1.0 订单，固定双内部页签并行。
- 调度器按两单一组：顺序创建和绑定页签、并行写入9个普通字段、串行操作3个活动页签控件、逐单12字段回读、重叠保存。
- 保存许可与具体任务绑定；未授权的保存按钮、F9及额外 `/coHandle` 请求继续被拦截。
- `errno=320` 进入人工队列，可继续保存、暂时跳过或放弃；填写失败支持人工修正后重新验证。
- 已实现中断检查点、防重复台账、成功页签回收、暂停/继续/停止和脱敏 CSV 报告。
- 离线逻辑测试通过：1/100/101条门禁、Schema、公式、枚举、响应分类、身份指纹、重复保护、双任务取队列和报告脱敏。
- 完整浏览器模拟通过：两套表单均12/12回读、A/B保存请求重叠、B触发 `errno=320` 后人工继续成功、仅关闭脚本自建成功页签、检查点完成后删除、防重复台账写入、刷新恢复不自动执行。
- 正式版未由本地自动测试直接连接真实车满满；实站能力建立在下述已完成探针和单条保存结果上。

## 2026-08-14 — 最终并行探针：核心结论完成

- A、B 两个新增内部页签拥有独立且持续连接的表单 DOM。
- 两套表单全部12个字段回读成功。
- 动态到站等自定义控件必须短暂激活对应内部页签，正式版因此使用活动页签互斥锁。
- A、B 保存请求成功发生时间重叠。
- A 返回保存成功并取得订单身份；B 返回 `errno=320`“是否继续保存运单？”，证明正式版必须实现人工确认分支，不能盲目自动重试。

## 2026-08-14 — 单页面内部页签基础探针：成功（历史记录）

- v0.2.0 实站报告结论为 `possible`：两个新增内部页签分别拥有独立 `.order-content.skin-border` 表单。
- 两套表单的 12 个关键 `data-path` 均同时保持连接；非活动表单不可见但未销毁。
- 非活动普通文本字段可直接写入，不影响活动表单；来回切换后两边标记均保持。
- 运行前已有的一套表单被正确解释为基线，因此全局字段总数为基线 1 + 探针 2 = 3。
- 后续 v0.3.0 最终探针结果已记录在本文件顶部。

## 2026-08-14 — 串行批量自动保存测试版：历史路线

- v0.1.8 由已验证的 v0.1.7 直接升级，支持一次导入最多两条虚构订单。
- 执行顺序为逐条创建、填写、回读、保存并等待明确结果。
- `decision_required` 或结果不确定时立即停止剩余队列。
- 离线语法、响应分类、两条导入和最大条数门禁测试已通过。
- 实站运行中第一条保存成功，第二条因切页后的字段定位问题停止；随后虽进入内部页签并行探针阶段，但最终由正式 v1.0.13 严格串行架构取代。

## 2026-08-14 — 单条自动填写并保存：成功

- 用户使用 v0.1.7 独立测试脚本和单行测试 Excel 完成真实网站测试。
- 脚本成功创建订单页签、填写全部字段并保存订单。
- 离线语法、响应分类、单条导入门禁及工作簿结构检查同时通过。
- 本次结果证明单条自动填写和保存闭环可行，不代表串行批量保存或并行保存已经验证。
- v0.1.6 的“批量创建并填写成功”结论保持不变。

## 2026-08-14 — 自动创建并填写表格：成功

用户在真实车满满系统中使用 v0.1.6 油猴脚本完成 6 条虚构订单测试，确认所有数据均成功填入。

通过项：

- 一份 Excel 导入 6 条记录。
- 每批创建并填写 2 个系统内部开单页签。
- 到站按 Excel 文本动态搜索并选择唯一精确候选。
- “送货/自提”和“现付”下拉项被网站确认选中。
- 发货人、收货人、手机号、货物、包装、件数、重量、体积和运费均成功写入。
- 空手机号、空包装、空重量和空体积未被错误写成 `null`、`undefined` 或 `0`。

未覆盖项：

- 自动保存和并发保存。
- 到付、欠付、回付及返款组合。
- 重名地点的人工消歧。

该状态只证明“自动创建并填写表格”成功，不代表自动保存已经获准或验证。
