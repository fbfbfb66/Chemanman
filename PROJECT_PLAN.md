# 车满满订单自动化项目计划

> 本计划以 2026-08-14 用户确认的最终业务流程为准。细节尚未确认的部分只记录方向，不提前扩大设计。

## 1. 最终目标

项目由两个相互独立、通过 Excel 交换数据的工具组成。

### 1.1 手机工具

手机工具负责：

1. 拍摄或选择真实托运单图片；
2. 使用 AI/OCR 提取订单信息；
3. 将识别结果展示给操作人员快速核对和修改；
4. 只有在人工确认后才能导出 Excel；
5. 导出的 Excel 符合浏览器脚本约定的数据格式。

手机工具不直接登录车满满，也不直接创建或保存网站订单。

### 1.2 浏览器脚本

浏览器脚本负责：

1. 导入手机工具或人工制作的 Excel；
2. 在执行前校验整份数据；
3. 为每条数据创建一个车满满订单页签；
4. 自动填写对应字段并回读核对；
5. 自动保存填写完成且校验通过的订单；
6. 显示每条订单的执行结果，并允许导出核对报告；
7. 严格逐条执行；上一单得到明确保存成功响应后保留页签，等待唯一创建入口稳定后才创建下一单，确保运单号正确递增。

脚本不负责从图片识别信息，也不绕过网站登录、权限、验证码或业务风控。

## 2. 完整业务流程

```text
真实托运单
  → 手机拍照
  → AI/OCR 提取字段
  → 人工核对与修正
  → 导出标准 Excel
  → 浏览器脚本导入并整批预检
  → 创建订单页签
  → 自动填写并回读验证
  → 自动保存
  → 输出成功、失败或待人工确认的执行报告
```

Excel 是手机工具与浏览器脚本之间的正式交换格式。两个工具只依赖约定的 Excel Schema，不直接共享账号、Cookie、网站内部字段或本地数据库。

## 3. 当前完成状态

当前正式实现为油猴脚本 v1.0.13 严格串行版；支持缓存 fetch 响应捕获、保存后刷新冷却、完整运单号DOM重绑、新运单活动状态确认、网站自动到站菜单保留、分阶段DataList/Select打开、跨订单复用候选DOM、相同到站逐字重触发、两个枚举控件焦点重试，以及真实焦点浮层 `.fn-dropdown__menu` 的优先定位。v0.1.6、v0.1.8 和 v0.3.0 分别保留为只填表、串行保存和并行诊断历史基线。

v1.0.13 已完成最终实站验收：TEST-0001～TEST-0006 连续 6 条全部保存成功，72/72 个字段通过回读，6 次保存均取得 HTTP 200、`errno=0`，运单号后缀 071～076 严格递增。当前定义范围内的正式批量串行流程全部功能测试成功。

已经通过真实页面验证：

- 导入一份包含 6 条虚构订单的 XLSX；
- 每批创建并填写 2 个网站内部订单页签；
- 6/6 条订单均完成创建和字段填写；
- 到站动态候选能够精确选择并得到网站确认；
- 支持送货、自提和现付；
- 支持已纳入表格的文本、数字及空可选字段；
- 能够导出字段级核对报告；
- 未授权的保存按钮、F9 和额外保存请求会被拦截；脚本授权的自动保存已通过连续 6 条实站验证。

实站已证明批量填写、单条保存、双内部页签表单隔离、完整字段回读和保存请求重叠。后续又确认：上一订单未保存时继续创建，网站会复用相同运单号；只有上一单保存后再次创建，号码才递增。因此订单级并行被否决，正式版固定串行。

## 4. 当前范围

### 4.1 浏览器脚本近期范围

- 延续现有油猴脚本的页面自动化方式；
- 使用本地 XLSX 作为导入格式；
- 支持单货物行、单到站和当前已验证字段；
- 增加自动保存；
- 固定并发数为 1，不保留订单级并行；
- 每单先读取并校验页面运单号，再填写、回读和保存；成功页签保留；
- 提供开始、停止、进度、错误提示和结果导出；
- 任何字段填写或回读失败时，不保存该条订单。

### 4.2 手机工具后续范围

- 拍照或选择图片；
- AI/OCR 字段提取；
- 人工核对和修改；
- 必填项与格式校验；
- 导出与脚本兼容的 XLSX。

### 4.3 暂不确定的细节

以下内容等待后续业务讨论，不在当前阶段提前定案：

- 手机平台、开发框架及 AI/OCR 服务；
- 图片和识别结果的保存周期；
- 更多付款方式、返款及其他财务组合；
- 多货物行、多到站等复杂订单；
- 常用客户匹配和历史资料复用；
- 是否需要扩展超过 100 条的单批容量（当前固定最多 100 条且保持串行）；
- 失败订单的人工处理界面细节。

## 5. Excel 交换契约

当前 Schema 版本为 `v1.0`，以现有测试 Excel 和 v0.1.6 脚本为基线。

当前字段包括：

- `schema_version`
- `batch_id`
- `source_record_id`
- `source_label`
- `destination_text`
- `delivery_type`
- `sender_name`
- `receiver_name`
- `receiver_mobile`
- `goods_name`
- `package`
- `quantity`
- `weight`
- `volume`
- `freight`
- `payment_type`

约束：

- `source_record_id` 在同一文件中必须唯一；
- 同一文件使用相同的 `batch_id`；
- 不接受公式单元格；
- 必填字段、枚举、数量和金额在页面操作前完成校验；
- 空值必须保持为空，不得写成字符串 `null` 或 `undefined`；
- 手机工具新增字段时必须提升 Schema 版本或保持向后兼容；
- 网站内部 ID、Cookie、Token 和会话参数不得进入 Excel。

## 6. 浏览器脚本执行原则

### 6.1 单条订单流程

每条订单按以下顺序执行：

```text
待处理
  → 创建订单页签
  → 定位字段
  → 填写字段
  → 回读验证
  → 保存
  → 确认保存结果
```

只有全部字段通过回读验证后才能保存。保存结果无法确认时，该条订单必须标记为“结果不确定”，不得立即重复保存。

### 6.2 整批流程

- 导入后先完成整批预检，再允许开始；
- 每条订单拥有独立状态和错误信息；
- 一条失败不应破坏其他尚未开始的订单数据，但必须解决或明确放弃后才继续；
- 用户停止后不再创建新订单；
- 已经进入保存阶段的订单必须等待明确结果或标记为结果不确定；
- 最终报告至少区分：填写失败、保存成功、保存失败、需要人工处理、结果不确定。

### 6.3 保存门禁

在自动保存开发和验证期间：

- v0.1.6 继续保留为“只填表不保存”的稳定版本；
- 新版本首先只允许一条虚构测试订单保存；
- 明确识别网站成功、业务拒绝、登录失效和无法确认结果；
- 保存动作不得对填写失败的订单执行；
- 未确认保存是否成功时禁止自动重试；
- 通过受控测试后再开放整批串行保存。

这些门禁服务于可靠实现最终功能，不改变“脚本最终需要自动保存”的目标。

## 7. 串行执行与运单号门禁

网站实测表明，未保存上一订单时创建下一单会复用相同运单号。因此正式架构不再保留订单级并行。

### 7.1 唯一执行顺序

```text
创建 → 读取运单号 → 填写 → 12字段回读 → 保存 → 明确成功 → 保留页签 → 等待创建入口稳定 → 下一单
```

- 任意时刻最多一个未完成订单和一个保存请求；
- 当前单未明确成功，或未明确放弃并关闭前，不得创建下一单；
- `errno=320`、填写失败和明确保存失败均暂停整批，等待处理；
- 保存结果不确定时停止，并要求先查询订单列表。

### 7.2 运单号校验

- 运单号只能在当前表单根容器内读取；
- 无法唯一读取非空号码时，在填写前停止；
- 下一单号码必须不同；同长度纯数字还必须大于上一已保存号码；
- 报告只输出号码后缀和哈希，完整号码只存在未完成检查点中。

### 7.3 成功页签保留与创建入口门禁

- `errno=0` 且响应可解析时直接标记成功，并给页签增加本批次完成标记；成功页签不自动关闭；
- 保存响应处理完全结束后，最多等待15秒，要求唯一的“创建运单”入口连续稳定500ms；
- 优先定位 `i.fn-icon.fn-icon-file-add` 所属链接，再以精确文字作为兼容回退；出现多个入口立即暂停；
- 每次创建只单击一次，并观察新增页签/表单。没有观察到结果时进入人工处理，不盲目再次点击；
- 成功页签不参与空白草稿扫描；只自动关闭确认安全的同号空白重复页签，或用户明确放弃的未保存页签。

## 8. 手机工具设计原则

- AI/OCR 结果只能作为待审核草稿；
- 金额、手机号、到站、付款方式等关键字段必须醒目标示；
- 低置信度或无法识别的内容应留空或标记待确认，不能猜测；
- 用户确认前不能导出可执行 Excel；
- 导出前使用与浏览器脚本一致的字段和格式规则；
- 手机工具不保存车满满登录凭证，也不调用车满满保存接口；
- 真实单据和个人信息的存储、上传、保留和删除方案在选定 AI/OCR 服务前单独确认。

## 9. 开发阶段

### 阶段 A：批量填表基线（已完成）

成果：v0.1.6 已完成 XLSX 导入、整批预检、每批两条创建与填写、字段回读和报告导出，真实页面 6/6 条测试通过。

### 阶段 B：单条自动保存（已完成）

目标：让一条通过填写和回读验证的虚构订单自动保存，并取得明确结果。

结果：2026-08-14，用户使用 v0.1.7 在真实网站完成单条创建、填写和保存，确认闭环成功。

验收：

- 填写失败不会触发保存；
- 保存成功后页面状态可确认；
- 登录失效、业务拒绝和未知响应有清晰提示；
- 保存结果不确定时不会自动再次保存；
- 测试订单有明确的核对和清理方式。

### 阶段 C：串行批量保存（已完成）

目标：将现有批量填表队列扩展为逐条创建、填写、验证和保存。

完成结果：v0.1.8 完成早期离线和实站探索；v1.0.13 已将空白页签接管、逐条填写、多层保存响应观察、保存后刷新冷却、新运单活动状态确认、表单DOM安全重绑、自动到站菜单保留、DataList/Select分阶段打开、焦点浮层/复用候选安全选择、到站逐字重触发、枚举焦点重试、脱敏诊断导出和稳定创建入口升级为正式架构。最终连续 6 条实站订单全部保存成功，72/72 字段通过。

验收：

- 一份测试 Excel 可以从第一条执行到最后一条；
- 每条订单均有独立结果；
- 失败、停止和结果不确定不会造成盲目重复保存；
- 报告能够用于人工核对网站订单。

### 阶段 D：受控并行（已否决为正式架构）

目标：从并行数 2 开始验证多个订单同时创建、填写和保存。

当前结论：v0.2.0/v0.3.0 证明双表单 DOM 隔离和保存请求重叠，但后续运单号实测发现未保存时新建会复用号码，违反唯一订单门禁。v1.0.0 并行正式文件已移除，不再重启该路线。

验收：

- 不发生跨页签错填；
- 不发生订单号、客户、地点或保存结果串单；
- 并行结果与串行结果一致；
- 性能确有提升且网站运行稳定；
- 失败时可安全降级到串行。

### 阶段 E：浏览器脚本完善

目标：提高长期使用的稳定性和操作体验。

当前进展：v1.0.13 已实现 1–100 条导入、现有空白页签复用、同号空白重复页签清理、严格单任务调度、运单号递增门禁、单保存许可、页面 `Response.json/text` 响应观察、保存后3秒冷却、新运单活动状态确认、已活动页签零点击、同运单号DOM安全重绑、自动到站菜单零点击保留、DataList/Select分阶段打开、焦点浮层优先的新/复用候选双路径选择、到站逐字重触发、枚举焦点重试及资源证据、链路诊断JSON、成功页签保留、创建入口稳定门禁、暂停/继续/停止、人工处理、中断检查点、防重复台账和脱敏报告。

可能包括：

- 暂停、继续和停止；
- 批量大小和执行超时配置；
- 更清晰的执行进度；
- 失败行重新导出；
- 已确认安全的人工重试；
- 网站字段变化诊断；
- 更多经实际验证的业务字段和枚举。

### 阶段 F：手机工具

目标：完成拍照、AI/OCR 提取、人工核对和 Excel 导出，并与浏览器脚本联调。

进入条件：

- 浏览器脚本使用的 Excel Schema 基本稳定；
- 手机端技术方案和 AI/OCR 服务已确定；
- 真实单据的隐私处理方案已确认。

## 10. 测试策略

### 10.1 填表测试

- 必填字段和可选字段；
- 送货和自提；
- 唯一到站、无候选和重名候选；
- 空手机号、包装、重量和体积；
- 非法数字、重复记录和公式单元格；
- 网站字段或候选结构变化。

### 10.2 保存测试

- 保存成功；
- 页面校验拒绝；
- 登录失效；
- 网站业务提示需要人工决定；
- 网络中断、超时或响应无法解析；
- 用户停止时订单正处于保存阶段；
- 防止结果不确定订单被自动重复保存。

### 10.3 串行与运单号测试

- 039 明确保存成功后保留页签，创建入口稳定后才允许创建 040；
- 任意时刻最多一个未完成订单和一个保存请求；
- 相同号码、数字倒退、无法读取号码时在填写前停止；
- `errno=320`、填写失败和保存失败未解决前不得创建下一单；
- 创建入口未出现、出现多个，或单击后未观察到新页签/表单时立即暂停；
- 人工解决后自动继续，用户主动停止后不自动继续。

### 10.4 手机工具测试

- 清晰、倾斜、阴影、模糊和手写单据；
- 关键字段识别错误的人工修正；
- 缺失字段和低置信度提示；
- 导出的 Excel 可被脚本直接导入；
- Schema 版本不兼容时给出明确提示。

## 11. 安全与隐私

- 只在自有或明确授权的账号和业务范围内运行；
- 测试阶段只使用虚构订单；
- 原始 HAR、Cookie、Token 和完整请求不得提交到 Git；
- 脚本日志和报告尽量减少真实姓名、手机号等个人信息；
- 未确定保存结果的订单不得自动重试；
- 手机图片和识别数据的隐私方案必须在真实使用前确定；
- 不绕过验证码、登录、权限或网站风控。

## 12. 当前下一步

浏览器正式脚本开发与实站验收已经完成。后续顺序为：

1. 正式使用 v1.0.13，并保持 Excel Schema v1.0 稳定；
2. 遇到网站结构变化时优先导出脱敏诊断，不盲目重试结果不确定的订单；
3. 启动手机工具方案设计，完成拍照、AI/OCR 提取、人工核对和 Schema v1.0 Excel 导出；
4. 手机工具完成后，以其导出的 Excel 与当前正式脚本做端到端联调。

## 13. 变更记录

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-08-14 | 1.0 | 重构计划：明确“手机识别并人工审核导出 Excel，浏览器脚本批量创建、填写并保存”为最终完整流程；自动保存纳入脚本目标；并行执行改为验证后逐步开放的性能优化。 |
| 2026-08-14 | 1.1 | v0.1.7 单条自动创建、填写和保存通过真实网站测试；下一阶段调整为串行批量保存。 |
| 2026-08-14 | 1.2 | v0.1.8 两条订单串行自动保存测试版完成离线验证，等待真实网站测试。 |
| 2026-08-14 | 1.3 | 增加单页面内部页签并行门禁：不采用新浏览器标签页；先通过 v0.2.0 自动探针验证双表单 DOM 隔离。 |
| 2026-08-14 | 1.4 | v0.2.0 基础探针实站通过；升级 v0.3.0 最终探针，一次验证完整字段与重叠保存。 |
| 2026-08-14 | 1.5 | v0.3.0 最终报告确认双表单12字段和保存请求重叠；按实测约束交付正式 v1.0.0 双并行脚本，加入人工确认、中断恢复、防重复和脱敏报告。 |
| 2026-08-14 | 1.6 | 实站确认未保存上一单时新建会复用运单号；撤销 v1.0.0 并行正式版，交付 v1.0.1 严格串行架构及运单号递增、页签关闭门禁。 |
| 2026-08-14 | 1.7 | v1.0.4 改为保存成功后保留页签；依据完整 HAR 增加唯一创建入口15秒等待、500ms稳定门禁和单击后结果观察，避免页面切换期间漏找或重复创建。 |
| 2026-08-14 | 1.8 | v1.0.5 依据“自动流程.har”确认网站模块缓存 fetch；新增页面 Response.json/text 捕获、Performance 1.5秒宽限和脱敏诊断JSON导出。 |
| 2026-08-14 | 1.9 | v1.0.6 依据“自动流程1.0.5.har”确认065保存后已创建066，但后续 `/oinfo` 刷新替换DOM；增加保存后3秒冷却、完整运单号重绑和创建阶段诊断。 |
| 2026-08-14 | 2.0 | v1.0.7 依据“自动流程1.0.6.har”确认067创建/绑定成功，但相同到站复用了旧候选DOM；增加新节点优先与近控件顶层唯一复用候选回退。 |
| 2026-08-14 | 2.1 | v1.0.8 对齐v1.0.7诊断与最新HAR，确认068的联想接口成功但候选未渲染；增加控件状态重置、一次受控重试及脱敏资源时序证据。 |
| 2026-08-14 | 2.2 | v1.0.9 根据真实DOM确认焦点菜单为 `.fn-dropdown__menu`；候选优先限定在该可见浮层，并增加浮层可见性诊断。 |
| 2026-08-14 | 2.3 | v1.0.10 根据最新HAR确认第二单两轮到站查询成功但均不渲染菜单；到站重试改为逐字输入，并为送货/付款枚举增加一次焦点重试。 |
| 2026-08-14 | 2.4 | v1.0.11 根据真实DataList/Select DOM主动发送完整鼠标序列与安全方向键打开；新增第2至第4单首次菜单丢失的连续四单回归。 |
| 2026-08-14 | 2.5 | v1.0.12 根据实站观察确认新运单自动聚焦到站并打开菜单；禁止重复点击，主动打开改为逐阶段停止，并排除Select误认空菜单壳。 |
| 2026-08-14 | 2.6 | v1.0.13 创建绑定后确认目标页签/表单活动状态；已活动时所有switchToTask零点击，并记录焦点字段和菜单数。 |
| 2026-08-14 | 2.7 | v1.0.13 连续6条实站最终验收通过：6/6保存、72/72字段回读、HTTP 200且errno=0、运单号071～076递增；浏览器脚本阶段完成。 |
